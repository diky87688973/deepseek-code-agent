# 项目安全分析报告（数据层面）

> 分析日期：2026年  |  范围：DeepSeek Code Agent v1 数据安全

---

## 1. 数据流全景

```
用户输入
  │
  ▼
┌──────────────┐     ┌──────────────────┐
│  Web UI      │────▶│  FastAPI 服务端   │
│  (浏览器)     │◀────│  (SSE 流式响应)   │
└──────────────┘     └────────┬─────────┘
                              │
                    ┌─────────▼─────────┐
                    │  模型 API 调用     │
                    │  (DeepSeek 等)     │
                    └─────────┬─────────┘
                              │
                    ┌─────────▼─────────┐
                    │  本地工具执行      │
                    │  (文件读写/Shell)  │
                    └───────────────────┘
```

**数据流向：** 用户输入 → 浏览器 → 服务端 → LLM API / 本地工具 → 返回结果

---

## 2. 数据传输安全

### 2.1 客户端 ↔ 服务端

| 维度 | 现状 | 风险等级 |
|------|------|---------|
| 传输协议 | HTTP（明文） | 🟡 中 |
| 连接方式 | localhost 回环地址 | 🟢 低 |
| 暴露范围 | 默认绑定 127.0.0.1，仅本机可访问 | 🟢 低 |

**分析：** 服务默认监听 `127.0.0.1`，仅本机进程可访问，不经过网络传输。局域网或公网无法直接访问，因此 HTTP 明文传输在本场景下风险可控。

**建议：** 若需远程访问，应前置 Nginx 反向代理并启用 HTTPS；或在 `config.json` 中绑定 `0.0.0.0` 时自行配置防火墙规则。

### 2.2 服务端 ↔ LLM API

| 维度 | 现状 | 风险等级 |
|------|------|---------|
| 传输协议 | HTTPS（由 urllib 发起） | 🟢 低 |
| 认证方式 | Bearer Token（API Key） | 🟢 低 |
| 请求内容 | 对话消息 + 工具定义 | 🟡 中 |

**分析：** API 调用使用 HTTPS 加密传输，API Key 通过 `Authorization: Bearer` 头传递。对话内容会经加密传输到模型服务商，用户应确认服务商的数据处理政策。

---

## 3. 数据存储安全

### 3.1 敏感信息存储

| 数据类型 | 存储位置 | 加密方式 | 风险等级 |
|---------|---------|---------|---------|
| API Key | `config.json`（明文） | 无加密 | 🟠 高 |
| API Key | 环境变量 | 进程内存 | 🟡 中 |
| 对话记录 | `cache/sessions/*.json` | 加密存储 | 🟢 低 |
| UI 状态 | `cache/ui_state.json` | 加密存储 | 🟢 低 |
| 加密密钥 | `cache/session_encryption.key` | 文件权限 600 | 🟡 中 |

### 3.2 加密方案

| 平台 | 加密算法 | 密钥管理 |
|------|---------|---------|
| Windows | DPAPI（`CryptProtectData`） | 系统托管，绑定当前 Windows 用户 |
| Linux/macOS | HMAC-SHA256 + XOR 流加密 | 随机 32 字节密钥 + 应用熵 HMAC 派生 |

```python
# 加密流程图
明文 JSON → DPAPI / local 加密 → Base64 编码 → 带魔数标识的 JSON 包 → 写入磁盘
```

**分析：**
- ✅ Windows DPAPI 使用系统级加密，密钥由操作系统管理，安全性高
- ✅ 加密数据携带魔数标识 `__deepseek_code_agent_session_encrypted__`，可区分加密/明文
- ✅ 加密密钥文件设置 `chmod 600`，仅当前用户可读
- ⚠️ `config.json` 中 API Key 为明文存储，是主要风险点

**建议：**
1. `config.json` 文件权限应设为仅当前用户可读写（Windows: 右键安全设置；Linux: `chmod 600 config.json`）
2. 可选方案：通过环境变量 `CHAT_API_KEY` 注入，避免 Key 落盘

---

## 4. 工具执行安全

### 4.1 Shell 命令执行

| 机制 | 说明 |
|------|------|
| `safeMode` | 默认开启，拒绝高危 Shell 元字符（管道/重定向/链式执行） |
| 用户授权 | 必须用户明确说出「我允许你执行命令」才可调用 |
| 授权生命周期 | 仅当次有效，每次需重新授权 |
| 最低优先级 | `run_command` 在所有工具中优先级最低，优先使用专用工具 |

### 4.2 文件操作安全

| 机制 | 说明 |
|------|------|
| 逻辑删除 | `file_ops` 删除 / `delete_file` 将文件移入安全删除区，非永久 unlink |
| 回收站路径 | `DATA_ROOT/AI_安全删除回收站/` |
| 写入限制 | Plan 模式拒绝所有写操作 |
| Tool-List 校验 | Execute 模式要求必须存在 Todo-List 才允许写操作 |

### 4.3 工具调用审计

| 机制 | 说明 |
|------|------|
| 控制台日志 | `AGENT_CONSOLE_LOG=1` 时输出每次工具调用的完整入参/出参 |
| 失败调试日志 | `AGENT_TOOL_DEBUG=1` 时，工具失败自动写入 `DATA_ROOT/debug/` |
| 调用记录 | 每次 tool_call 的 api_name、script、ok 状态记录到对话历史 |

---

## 5. 会话与权限隔离

### 5.1 多会话隔离

| 维度 | 实现 |
|------|------|
| 对话隔离 | 每会话独立 `CONVERSATIONS[cid]` 内存空间 |
| Todo-List 隔离 | 每会话独立 `TODO_LISTS[cid]` |
| 模式隔离 | 每会话独立 `CONVERSATION_MODES[cid]` |
| 知识库勾选 | 每会话独立 `_KB_CHECKED_STATE[cid]` |
| 并发控制 | 每会话 `RLock`，同一时间只处理一个请求 |

### 5.2 停止机制

- 用户可通过 `POST /api/chat/stop` 停止正在执行的会话
- 停止后自动回滚到本轮开始前的消息状态（`rollback_messages`）
- 不完整的 tool_calls 链自动裁剪（`_tail_drop_incomplete_tool_assistant`）

---

## 6. 数据隐私

### 6.1 数据收集

| 项目 | 情况 |
|------|------|
| 遥测/统计上报 | ❌ 无任何数据上报 |
| 用户行为追踪 | ❌ 无埋点/追踪 |
| 本地日志 | ✅ 可选开启，仅写入本地 `DATA_ROOT/` |
| API 调用日志 | ✅ 可选开启，仅写入本地 stderr 或 debug 目录 |

### 6.2 数据清除

| 数据类型 | 清除方式 |
|---------|---------|
| 对话记录 | 删除 `cache/sessions/` 目录 |
| 加密密钥 | 删除 `cache/session_encryption.key`（注意：已加密的对话将无法解密） |
| 全部数据 | 删除 `DATA_ROOT/` 目录 |

---

## 7. 供应链安全

| 依赖 | 风险 | 缓解措施 |
|------|------|---------|
| fastapi / uvicorn | Web 框架漏洞 | 锁定版本 `==0.103.2` / `==0.22.0` |
| pydantic | 反序列化风险 | 严格 JSON Schema 校验 |
| pystray / Pillow | 系统托盘依赖 | 仅 Windows 运行时可选的 UI 组件 |
| urllib（标准库） | HTTP 请求 | 仅发起 HTTPS 请求到用户配置的 API 地址 |

---

## 8. 风险评级汇总

| 风险项 | 等级 | 说明 |
|--------|------|------|
| API Key 明文存储 | 🟠 高 | `config.json` 未加密，建议设文件权限或使用环境变量 |
| 本地 HTTP 无加密 | 🟡 中 | 仅限 localhost 时风险可控，远程访问需加 HTTPS |
| 对话内容送第三方 API | 🟡 中 | 取决于 LLM 服务商的数据处理政策 |
| 加密密钥文件泄露 | 🟡 中 | 文件权限 600 + 应用熵 HMAC 双重保护 |
| Shell 命令执行 | 🟡 中 | safeMode + 用户授权 + 审计日志三重防护 |
| 文件误删除 | 🟢 低 | 回收站机制可恢复 |
| 未授权访问 | 🟢 低 | 默认仅 127.0.0.1 监听 |
| 数据泄露至第三方 | 🟢 低 | 无遥测、无上报 |

---

## 9. 安全建议

| 优先级 | 建议 |
|--------|------|
| 🔴 P0 | `config.json` 文件权限设为仅当前用户（`chmod 600 config.json`），或改用环境变量注入 API Key |
| 🟡 P1 | 如需远程访问，前置 Nginx 反向代理 + HTTPS |
| 🟡 P1 | 定期轮换 API Key |
| 🟢 P2 | 敏感对话结束后，定期清理 `cache/sessions/` 历史记录 |
| 🟢 P2 | 设置 `AGENT_TOOL_DEBUG=0` 关闭调试日志（生产环境） |

---

> 本报告仅覆盖数据安全层面，不含代码安全审计。
