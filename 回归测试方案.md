# 回归测试方案 — Code Web Agent 工具库

> 版本：v2 | 用途：每次修改 agent_main/main 后执行回归
> 执行命令：`回归` 或 `按回归测试方案执行`

---

## 补全测试用例规则

1. 每个工具必须有测试用例覆盖，缺失的需补全。
2. 单参数工具至少 1 条用例。
3. 多参数工具：每个参数至少被一条用例覆盖；组合参数需追加场景用例。用例数不设上限，参数全覆盖为止。
4. 每条用例必须写明输入参数与验收标准。
5. 验收标准不能仅依赖工具返回的 `ok` 值，必须通过旁路检查验证实际结果。例如测试新增文件后，用 `glob_files`/`read_file` 确认磁盘文件真实存在。
6. 必要时增加负向用例（如故意传错误路径），验证工具容错行为。
7. 写操作类用例必须覆盖 Plan 模式拦截场景：先切 Plan，再调用写工具，验证被拦截。
8. 危险工具（如 `run_command`、`python_inline`）需用户明确授权后方可执行。

---

## 前置准备（全局）

在执行测试前，先创建以下测试资源：

```bash
# 测试目录
E:\AgentTest\
├── src\
│   ├── hello.txt       # 内容: "Hello World"
│   ├── config.json     # 内容: '{"name": "test", "version": 1}'
│   └── code.py         # 内容: 'def foo():\n    return 42\n\ndef bar(x):\n    return x + 1'
├── archive\             # 空目录，用于解压
└── images\
    ├── test_ocr.png    # 来源：复制自项目 res/img/test_ocr.png（含文字的测试图片）
    └── test_blank.png  # 纯白测试图片（1x1 像素 PNG），用于测试无文字识别
```

---

## 测试用例

### 1. archive — 压缩包处理（5用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 1.1 | list 不存在文件 | `action=list` `source=E:\不存在.zip` | 返回 `FileNotFoundError` | ✅ 通过——预期拦截/报错 |
| 1.2 | create zip 包 | `action=create` `source=E:\AgentTest\src\hello.txt` `dest=E:\AgentTest\hello.zip` | `ok=true`，且磁盘 `E:\AgentTest\hello.zip` 已存在 | ✅ 通过 |
| 1.3 | list zip 包内容 | `action=list` `source=E:\AgentTest\hello.zip` | entries 含 `hello.txt` | ✅ 通过 |
| 1.4 | extract 解压 | `action=extract` `source=E:\AgentTest\hello.zip` `dest=E:\AgentTest\archive` | `extractedCount>=1`，且磁盘 `E:\AgentTest\archive\hello.txt` 存在 | ✅ 通过 |
| 1.5 | Plan 模式拦截 extract | 先切 Plan，再调 extract（同 1.4 参数） | 返回 `ModeConflict`，磁盘解压目录不变 | ✅ 通过——预期拦截/报错 |

### 2. run_command — 命令执行（5用例）

> 需要用户授权才可执行，测试前明确说"我允许执行命令"

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 2.1 | 简单命令 | `command=echo hello` | `exit_code=0` `stdout` 含 `hello` | ✅ 通过 |
| 2.2 | 工作目录 | `command=cd` | `exit_code=0` | ✅ 通过 |
| 2.3 | 失败命令 | `command=exit 1` | `exit_code=1` | ✅ 通过——预期拦截/报错 |
| 2.4 | 命令黑名单拦截 | `command=rm -rf /` | 返回命令黑名单拦截错误，未实际执行 | ✅ 通过——预期拦截/报错 |
| 2.5 | safeMode 拦截元字符 | `command=echo hello && dir` | 返回 safeMode 拦截错误 | ✅ 通过——预期拦截/报错 |

### 3. data_table — 表格数据处理（5用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 3.1 | preview 不存在文件 | `action=preview` `source=E:\不存在.csv` | 返回 `FileNotFoundError` | ✅ 通过——预期拦截/报错 |
| 3.2 | preview CSV 文件 | 先创建测试 CSV `E:\AgentTest\src\test.csv`（含列名行+数据行），`action=preview` `source=E:\AgentTest\src\test.csv` | 显示表头和数据行 | ✅ 通过 |
| 3.3 | filter 筛选 | `action=filter` `source=E:\AgentTest\src\test.csv` `filter_col=name` `filter_val=test` | 返回筛选结果 | ✅ 通过 |
| 3.4 | to_json 转换 | `action=to_json` `source=E:\AgentTest\src\test.csv` | 返回 JSON 格式内容 | ✅ 通过 |
| 3.5 | sheets 列工作表（xlsx） | `action=sheets` `source=E:\AgentTest\src\test.xlsx` | 返回工作表名列表（xlsx 不存在时返回合理错误） | ✅ 通过 |

### 4. env_probe — 环境探测（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 4.1 | 基本探测 | 无参数 | 返回 `platform`/`python`/`cwd` 字段 | ✅ 通过 |
| 4.2 | JSON 输出 | `json_out=true` | 返回统一 JSON 信封 | ✅ 通过 |
| 4.3 | 验证可用工具 | 检查 `data.available` | 含 `cmd`/`python` 等 | ✅ 通过 |

### 5. file_ops — 文件操作（5用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 5.1 | copy 文件（dry_run） | `action=copy` `source=E:\\AgentTest\\src\\hello.txt` `dest=E:\\AgentTest\\hello_copy.txt` `dry_run=true` | 返回预览信息，磁盘上 `hello_copy.txt` 不存在 | ✅ 通过 |
| 5.2 | copy 文件（执行） | 同上 `dry_run=false`（Execute 模式） | `ok=true`，且磁盘 `E:\\AgentTest\\hello_copy.txt` 存在 | ✅ 通过 |
| 5.3 | rename 文件 | `action=rename` `source=E:\\AgentTest\\hello_copy.txt` `dest=E:\\AgentTest\\hello_renamed.txt` `dry_run=false` | 磁盘上旧名不存在，新名存在 | ✅ 通过 |
| 5.4 | move 文件 | `action=move` `source=E:\\AgentTest\\hello_renamed.txt` `dest=E:\\AgentTest\\moved\\hello.txt` `dry_run=false` | 磁盘上原路径无文件，目标路径存在 | ✅ 通过 |
| 5.5 | Plan 模式拦截 copy | 先切 Plan，再调 `action=copy` `dry_run=false` | 返回 `ModeConflict`，磁盘无变化 | ✅ 通过——预期拦截/报错 |

### 6. grep_files — 内容搜索（4用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 6.1 | 目录字面搜索 | `path=E:\\AgentTest\\src` `pattern=def` | 命中含 `code.py`，返回 `region_start`/`region_end` | ✅ 通过 |
| 6.2 | 正则搜索 | `path=E:\\AgentTest\\src` `pattern=def\\w+` `regex=true` | 匹配 `def foo` / `def bar` | ✅ 通过 |
| 6.3 | 带上下文搜索 | `path=E:\\AgentTest\\src\\code.py` `pattern=return` `context_lines=1` | 每条命中附带上下各 1 行 | ✅ 通过 |
| 6.4 | 无匹配 | `path=E:\\AgentTest\\src` `pattern=XXXXXXXXX` | `match_count=0` | ✅ 通过 |

### 7. replace_in_file — 文件内容替换（6用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 7.1 | 字面替换 dry_run | `path=E:\\AgentTest\\src\\code.py` `old_text=42` `new_text=0` `dry_run=true` | 返回 `diffText`，磁盘文件未改 | ✅ 通过 |
| 7.2 | 字面替换执行 | 同上 `dry_run=false`（Execute 模式） | 文件内容 `42` 变为 `0` | ✅ 通过 |
| 7.3 | rules 批量替换 | `path=E:\\AgentTest\\src\\code.py` `rules=[{old_text:foo,new_text:FOO},{old_text:bar,new_text:BAR}]` `dry_run=false` | 文件中 `foo`→`FOO`，`bar`→`BAR` | ✅ 通过 |
| 7.4 | 行区间替换 | `path=E:\\AgentTest\\src\\hello.txt` `line_start=1` `line_end=1` `new_text=Replaced` | 第 1 行被替换 | ✅ 通过 |
| 7.5 | region 区间替换 | 先 `find_in_file` 得 region，传 `region_start`/`region_end`/`new_text` | 仅区间内变更 | ✅ 通过 |
| 7.6 | Plan 模式拦截 | 先切 Plan，再调 7.2 参数 | `ModeConflict`，磁盘未改 | ✅ 通过——预期拦截/报错 |

### 8. image_ocr — 图片文字识别（3用例）

> **测试图片位置：** 项目内自带 `res/img/test_ocr.png`（DeepSeek 官网首页截图，含中英文）
> 测试时直接引用该项目路径即可，无需复制到额外目录

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 8.1 | 不存在图片 | `source=E:\\不存在.png` | FileNotFoundError | ✅ 通过——预期拦截/报错 |
| 8.2 | 有效图片（自动检测引擎） | `source=<项目根>/res/img/test_ocr.png` | 返回识别文本 | ✅ 通过 |
| 8.3 | 指定 easyocr 引擎 | `source=<项目根>/res/img/test_ocr.png` + `engine=easyocr` | 返回识别结果或引擎缺失提示 | ✅ 通过 |

### 9. ip_geolocate — IP 定位（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 9.1 | 查询本机 | 无参数 | 返回 `ip`/`city`/`country` 字段 | ✅ 通过 |
| 9.2 | 指定公网 IP | `ip=8.8.8.8` | 返回 Mountain View / Google LLC | ✅ 通过 |
| 9.3 | 本地回环地址 | `ip=127.0.0.1` | 视为未传 IP，自动查本机出口 IP | ✅ 通过 |

### 10. open_meteo_weather — 天气查询（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 10.1 | 按城市查询 | `location=北京` | 返回 `current`/`daily` 天气数据 | ✅ 通过 |
| 10.2 | 按经纬度查询 | `latitude=39.9042` `longitude=116.4074` `forecast_days=1` | 返回北京当天天气 | ✅ 通过 |
| 10.3 | 不存在的城市 | `location=不存在的城市名xxxx` | 返回 `NotFound` | ✅ 通过——预期拦截/报错 |

### 11. apply_patch — 补丁应用（4用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 11.1 | 缺 patch 参数 | `path=E:\AgentTest` 无 `patch_text` 也无 `patch_file` | 参数错误 | ✅ 通过——预期拦截/报错 |
| 11.2 | dry_run 预览 | `path=E:\AgentTest` `patch_text=--- a/test\n+++ b/test\n@@ -1 +1 @@\n-old\n+new` `dry_run=true` | 校验通过，返回 `diff_text` | ✅ 通过 |
| 11.3 | 实际应用 | 同上 `dry_run=false`（Execute 模式），目标文件存在 | 文件内容被更新 | ✅ 通过 |
| 11.4 | Plan 模式拦截 | 先切 Plan，再调 11.3 参数 | `ModeConflict`，磁盘未改 | ✅ 通过——预期拦截/报错 |

### 12. python_inline — 内联 Python（4用例）

> 需要用户授权才可执行，测试前明确说"我允许执行命令"

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 12.1 | 简单计算 | `code=print(1+1)` | `stdout=2` | ✅ 通过 |
| 12.2 | 文件写入 | `code=open('E:/AgentTest/src/py_test.txt', 'w').write('test')` | 磁盘 `py_test.txt` 存在且内容为 `test` | ✅ 通过 |
| 12.3 | 异常代码 | `code=1/0` | `exit_code=1`，`stderr` 有异常信息 | ✅ 通过——预期拦截/报错 |
| 12.4 | Plan 模式拦截 | 先切 Plan，再调 12.1 | 返回 `ModeConflict` | ✅ 通过——预期拦截/报错 |

### 13. regex_locate — 正则定位（4用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 13.1 | 简单匹配 | `path=E:\AgentTest\src\code.py` `pattern=def` | 匹配函数定义行 | ✅ 通过 |
| 13.2 | 忽略大小写 | `path=E:\AgentTest\src\code.py` `pattern=DEF` `ignore_case=true` | 匹配 `def` 忽略大小写 | ✅ 通过 |
| 13.3 | 无匹配 | `path=E:\AgentTest\src\code.py` `pattern=不存在的模式` | `count=0` | ✅ 通过 |
| 13.4 | 多文件递归 | `path=E:\AgentTest\src` `pattern=def` `recursive=true` `glob_pattern=*.py` | 命中所有 `.py` 文件 | ✅ 通过 |

### 14. run_type — 模式管理（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 14.1 | 查询当前模式 | 无参数 | 返回 `run_type`/`action` 字段 | ✅ 通过 |
| 14.2 | 请求切换 Plan | `run_type=plan` | 模式已切换为 plan | ✅ 通过 |
| 14.3 | 请求切换 Execute | `run_type=execute` | 模式已切换为 execute | ✅ 通过 |

### 15. read_file — 文件读取（5用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 15.1 | 读全文 | `path=E:\AgentTest\src\hello.txt` | 返回 `content` 含 `Hello World` | ✅ 通过 |
| 15.2 | 行区间读取 | `path=E:\AgentTest\src\code.py` `line_start=1` `line_end=3` | 返回第 1-3 行内容 | ✅ 通过 |
| 15.3 | 字符区间读取 | `path=E:\AgentTest\src\hello.txt` `char_start=0` `char_end=5` | 返回 `Hello` | ✅ 通过 |
| 15.4 | 自动编码探测 | `path=E:\AgentTest\src\hello.txt` `encoding=auto` | 正常读取不报错 | ✅ 通过 |
| 15.5 | 文件不存在 | `path=E:\不存在的文件.txt` | `FileNotFoundError` | ✅ 通过——预期拦截/报错 |

### 16. write_file — 文件写入（4用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 16.1 | dry_run 预览 | `path=E:\AgentTest\src\write_test.txt` `content=write test` `dry_run=true` | 返回 `diff_text`，磁盘文件不存在 | ✅ 通过 |
| 16.2 | 真实写入 | 同上 `dry_run=false`（Execute 模式） | `ok=true`，磁盘 `write_test.txt` 存在且内容匹配 | ✅ 通过 |
| 16.3 | create_only 防覆盖 | 目标已存在时 `create_only=true` | 文件已存在错误 | ✅ 通过——预期拦截/报错 |
| 16.4 | Plan 模式拦截 | 先切 Plan，再调 16.2 参数 | `ModeConflict`，磁盘未改 | ✅ 通过——预期拦截/报错 |

### 17. text_diff — 文本对比（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 17.1 | 相同文本 | `left_text=abc` `right_text=abc` | `summary.same=true` | ✅ 通过 |
| 17.2 | 不同文本 | `left_text=abc` `right_text=abd` | `summary.same=false`，diff 含差异行 | ✅ 通过 |
| 17.3 | 文件对比 | `left_file=E:\AgentTest\src\hello.txt` `right_file=E:\AgentTest\src\code.py` | diff 正确 | ✅ 通过 |

### 18. file_search — 全文搜索（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 18.1 | 字面搜索 | `path=E:\AgentTest\src` `pattern=def` | 返回匹配行 | ✅ 通过 |
| 18.2 | 正则搜索 | `path=E:\AgentTest\src` `pattern=def\w+` `regex=true` | 返回函数名匹配 | ✅ 通过 |
| 18.3 | 带上下文搜索 | `path=E:\AgentTest\src` `pattern=def` `context_lines=2` | 返回上下文行 | ✅ 通过 |

### 19. todo_list — 执行清单（5用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 19.1 | create | `action=create` `items=["任务1","任务2"]` | 返回 `list_id` | ✅ 通过 |
| 19.2 | add_item | `action=add_item` `text=任务3` | 清单含 3 项 | ✅ 通过 |
| 19.3 | check | `action=check` `indices=[0]` | 任务1 已勾选 | ✅ 通过 |
| 19.4 | query | `action=query` | 返回清单完整状态 | ✅ 通过 |
| 19.5 | close | `action=close` | 清单已关闭 | ✅ 通过 |

### 20. unified_diagnose — 统一诊断（4用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 20.1 | 诊断目录 | `path=E:\AgentTest\src` `limit=5` | 返回诊断结果 | ✅ 通过 |
| 20.2 | 语法错误文件 | 创建含语法错误的 `.py` 文件后诊断 | 发现语法错误 | ✅ 通过——预期拦截/报错 |
| 20.3 | no_ruff 模式 | `path=E:\AgentTest\src` `no_ruff=true` | `ruff_attempted=false` | ✅ 通过 |
| 20.4 | 路径不存在 | `path=E:\不存在的目录` | `NotADirectoryError` | ✅ 通过——预期拦截/报错 |

### 21. user_confirm — 用户确认（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 21.1 | 带 confirm 回传 | `title=测试` `confirms=["A","B"]` `confirm=A` | `data.confirm=A` | ✅ 通过 |
| 21.2 | 无 confirm（首次） | `title=测试` `confirms=["A","B"]` | 返回 `E_USER_CONFIRM_REQUIRED` | ✅ 通过——预期拦截/报错 |
| 21.3 | 多选场景 | `title=测试` `confirms=["X","Y","Z"]` `confirm=Y` | `data.confirm=Y` | ✅ 通过 |

### 22. web_fetch — 网页抓取（4用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 22.1 | 无效 URL | `url=https://不存在的域名.xyz` `timeout_sec=5` | URL 错误或超时 | ✅ 通过——预期拦截/报错 |
| 22.2 | 有效 URL | `url=https://httpbin.org/get` `timeout_sec=10` | 返回内容 | ✅ 通过 |
| 22.3 | max_chars 截断 | `url=https://httpbin.org/get` `max_chars=50` | `truncated=true` | ✅ 通过 |
| 22.4 | 空 URL | `url=` | 参数错误 | ✅ 通过——预期拦截/报错 |

### 23. find_in_file — 单文件定位（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 23.1 | 字面定位 | `path=E:\AgentTest\src\code.py` `pattern=def` | 返回 `region_start`/`region_end` | ✅ 通过 |
| 23.2 | 指定 occurrence | `path=E:\AgentTest\src\code.py` `pattern=def` `occurrence=1` | 命中第 2 个 `def` | ✅ 通过 |
| 23.3 | 无匹配 | `path=E:\AgentTest\src\code.py` `pattern=XXXXXXXX` | `found=false` | ✅ 通过 |

### 24. delete_file — 安全删除（4用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 24.1 | dry_run 预览 | `path=E:\AgentTest\src\write_test.txt` `dry_run=true` | 返回 `would_move_to`，原文件存在 | ✅ 通过 |
| 24.2 | 真实删除 | 同上 `dry_run=false`（Execute 模式） | `ok=true`，磁盘原文件不存在 | ✅ 通过 |
| 24.3 | 文件不存在 | `path=E:\不存在的文件.txt` | `FileNotFoundError` | ✅ 通过——预期拦截/报错 |
| 24.4 | Plan 模式拦截 | 先切 Plan，再调 `path=...` `dry_run=false` | `ModeConflict`，文件未删 | ✅ 通过——预期拦截/报错 |

### 25. glob_files — 路径列举（4用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 25.1 | 列出所有文件 | `path=E:\AgentTest` `glob_pattern=*` | 返回文件列表 | ✅ 通过 |
| 25.2 | glob 过滤 | `path=E:\AgentTest` `glob_pattern=*.txt` | 只返回 `.txt` 文件 | ✅ 通过 |
| 25.3 | 递归列举 | `path=E:\AgentTest` `glob_pattern=**/*` `recursive=true` | 递归列出全部 | ✅ 通过 |
| 25.4 | 目录不存在 | `path=E:\不存在的目录` | 路径不存在 | ✅ 通过——预期拦截/报错 |

### 26. read_write — 管道式读写（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 26.1 | dry_run 复制 | `source_path=E:\AgentTest\src\hello.txt` `dest_path=E:\AgentTest\rw_copy.txt` `dry_run=true` | 返回预览，目标文件不存在 | ✅ 通过 |
| 26.2 | 真实复制执行 | 同上 `dry_run=false`（Execute 模式） | 磁盘目标文件存在且内容与源一致 | ✅ 通过 |
| 26.3 | 行区间复制 | `source_path=E:\AgentTest\src\code.py` `dest_path=E:\AgentTest\rw_snippet.txt` `line_start=1` `line_end=2` `dry_run=false` | 目标文件仅含源文件第 1-2 行 | ✅ 通过 |

### 27. git_workspace — Git 查询（2用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 27.1 | 非 Git 目录 | `path=E:\AgentTest` `mode=log` | 返回非 Git 仓库错误 | ✅ 通过——预期拦截/报错 |
| 27.2 | 正常 Git 仓库 | `path=<含 .git 的目录>` `mode=log` `log_max=5` | 返回最近提交记录 | ✅ 通过 |

### 28. stdio_utf8 — 标准输出编码（1用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 28.1 | 输出 UTF-8 测试 | 直接调用验证 UTF-8 输出兼容 | STDIO 为 UTF-8 编码 | ✅ 通过 |

### 29. tool_help_share — 工具帮助共享（1用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 29.1 | 查询工具帮助 | 直接调用查看帮助输出 | 返回统一的工具帮助信息 | ✅ 通过 |

### 30. command_safety — 命令安全检测（3用例）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|:---------:|:----:|
| 30.1 | 命令黑名单拦截 | 调用 `run_command` 执行 `rm` 命令 | 返回命令黑名单拦截 | ✅ 通过——预期拦截/报错 |
| 30.2 | safeMode 字符拦截 | 调用 `run_command` 执行含 `&&` 的命令 | 返回 safeMode 拦截 | ✅ 通过——预期拦截/报错 |
| 30.3 | 白名单命令放行 | 调用 `run_command` 执行 `echo safe` | 正常执行 | ✅ 通过 |

---

## 测试报告样例

以下为 apply_patch 的测试执行记录，展示结果表述规范：

| 用例 | 测试场景 | 测试输入 | 期望输出 | 实际输出 | 旁路检测 | 测试结果 |
|:----:|:--------|:---------|:---------|:---------|:---------|:--------:|
| 11.1 | 缺 patch 参数 | 不传 `patch_text`/`patch_file` | 报错提示必须提供 patch 内容 | `ok=false`，提示"patch_text 与 patch_file 必须且只能提供一个" | — | ✅ 通过 |
| 11.2 | dry_run 预览 | `patch_text=...` `dry_run=true` | 返回 `diff_text`，磁盘不变 | `ok=true`，返回 `diff_text` | `read_file` 确认文件未改 | ✅ 通过 |
| 11.3 | 实际应用补丁 | 同上 `dry_run=false`（Execute） | 文件内容被更新 | `ok=true`，内容已变 | `read_file` 确认 `old`→`new` | ✅ 通过 |
| 11.4 | Plan 模式拦截 | 切 Plan，调 `apply_patch` `dry_run=false` | 拦截，`ModeConflict`，文件不变 | `ok=false`，返回 `ModeConflict` | `read_file` 确认文件未改 | ✅ 通过 |
---

## 测试通过标准

- ✅ **测试通过**：工具行为符合预期（不论 `ok=true` 或 `ok=false`）
- ❌ **测试未通过**：工具行为不符合预期
- `ok` 返回值见各用例「工具返回」列
- **实际输出与旁路检测必须一致**：若旁路检测结果与实际输出矛盾，即使单方面都"正确"，也判为测试失败。
  例：删除文件时工具返回 `ok=false`（声称未删除），但旁路检测发现文件确实已消失 → ❌ 测试未通过（输出与事实不符）
- 需要用户授权的测试用 `⏸️` 标记

## 清理

测试完成后清理临时文件：使用 `delete_file` / `file_ops` 或资源管理器删除 `E:\AgentTest`（注意回收站策略以当前工具为准）。
