# 里程碑计划

> 详细变更以 [`版本日志.md`](版本日志.md) 为准（当前 **v1.4**，见 `agent_v3/version.py`）；本页按里程碑归纳当前产品形态与未来方向。  
> **当前稳定运行栈**：`deepseek_code_agent3.py` → `agent_v3/`（HTTP + 宿主循环 + 经典页 `/` + 沉浸页 `/immersive`）。  
> **回归**：用户永远按 [`回归测试方案.md`](回归测试方案.md) 验收**最新重构全栈（agent_v3）**。

---

## 已交付里程碑

| 版本 | 主题 | 要点 |
|------|------|------|
| v0.1 | 首版骨架 | Web 服务、四项演示工具、最小对话 UI |
| v0.2 | 本地文件工具 | read/write/replace、glob、file_ops、grep、目录检索 |
| v0.3 | 工具规范化 | `tool_list_agent.json` 集中注册、schema 描述、失败 tool_help |
| v0.4 | 协议与 SSE | 对话补全协议、SSE 事件流、聊天区 + 步骤面板 |
| v0.5 | 前缀缓存与用量 | 前缀稳定、缓存命中拆分、侧栏环形用量 |
| v0.6 | 摘要流水线 | 超阈值异步摘要、工具回合扩范围、并发锁 |
| v0.7 | 多标签与托盘 | 浏览器多会话标签、加密落盘、Windows 托盘 |
| v0.8 | 模式与知识库 | Auto/Plan/Execute、推理档位、知识库注入、斜杠命令 |
| v0.9 | 按会话模型 | 会话绑定模型、定价累计、流式账单拆分 |
| v1.0 | 上下文分段 | pure/full 双窗、远期折叠、context_layout 用量条 |
| v1.1 | 沉浸模式 | 多栏并行、`/immersive`、明暗主题、跨模式会话同步 |
| v1.2 | 可灵与导演技能 | `kling_generate`、Dreamina、AI 视频导演 Skill 体系 |
| v1.3 | Agent Session Mesh | `session_*` 协作、`requires_reply`、inbox 队列、`session_wait` 挂起恢复 |
| **v1.4** | **工程化 + agent_v3 宿主拆分（当前）** | 见下表 |

### v1.4 — 当前稳定版（agent_v3 运行栈）

| 方向 | 说明 |
|------|------|
| 宿主拆分 | 门面 `agent_v3/agent_core.py`；业务子模块 `agent_v3/core/*`；路由 `routes.py`；SSE `sse_events.py`；配置 `bootstrap.py` |
| 上下文引擎 | `util/context_manager_v2.py`；七段 context_layout（系统提示词 / 技能 / 知识库 / 对话摘要 / 远期记忆 / 近期记忆 / 模式） |
| 提示词体系 | `util/agent_prompt_constants.py` + `tool_list_agent.json` agent_hints；function 名与 schema 严格对齐 |
| 协作加固 | P0 `requires_reply` ephemeral 尾注；Execute 写盘须 `todo_list`；peer 连续 turn 限流 |
| 模式扩展 | 仅审查意图强制 Plan（`audit_intent_keys`）；写类工具默认 `dry_run=true` |
| 工具规模 | catalog 注册 **38** 个 function（见 `tools/tool_list_agent.json`） |
| 质量门禁 | `tests/` 单元测试；`agent_v3/check_symbols.py`；`scripts/check_agent_v3_health.py`（含 `session_*` 契约扫描）；[`回归测试方案.md`](回归测试方案.md) |
| 协作契约 | `session_*` 去 `action`；`session_wait` 仅 `suspend=true` 宿主挂起；`file_search` 宿主 gate |
| 语音 | TTS 串行合成（`util/tts/manager.py`）；侧栏开关 |

**主要代码落点**

| 模块 | 路径 |
|------|------|
| 入口 | `deepseek_code_agent3.py` |
| 宿主门面 | `agent_v3/agent_core.py`（聚合 `agent_v3/core/*`） |
| HTTP / SSE | `agent_v3/routes.py`、`agent_v3/http_app.py` |
| 协作状态 | `agent_v3/live_state.py` |
| 工具 catalog | `tools/tool_list_agent.json` |
| 配置 | `config.ini` + `util/config_loader.py` |
| 经典 UI | `res/js/agent-ui.js` |
| 沉浸 UI | `res/js/immersive/` |

---

## 未来规划

### v2.0 — 多模态 + 记忆系统

| 主线 | 内容 |
|------|------|
| 多模态入口 | ASR 语音输入、图片上传、多模态 LLM |
| 记忆模型 | 专用记忆写入/回放、向量检索增强 RAG |
| 嵌入式 SDK | Web 组件化、核心能力 C++ 化（旁支） |

### v2.x 旁支（按需）

决策控制系统（高频调度闭环）、运动控制/硬件接口、专用记忆夜间巩固训练等——见 [`进行中的拓展工具.md`](进行中的拓展工具.md) 能力缺口。

---

## 版本时间线

```
v0.1 ── … ── v1.0 ── v1.1 ── v1.2 ── v1.3 ── v1.4（当前）
                                              │
                                              ▼
                                    v2.0 多模态 + 记忆
```

---

## 版本号约定

| 段 | 含义 |
|----|------|
| v0.x | 能力积累 |
| v1.x | 稳定产品迭代 |
| v2.0 | 架构级升级（多模态 / 记忆） |

> 最后更新：2026-05-27
