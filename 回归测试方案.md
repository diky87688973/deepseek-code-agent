# 回归测试方案

> **用途**：大重构或改 core / catalog / prompt 后，由 **Agent 在终端自动执行** 回归（不是让用户手跑）。  
> **自描述**：执行者读完本文后 **必须立刻用 `run_command` 跑 Layer 0**，不得只写「建议你执行…」。  
> **用户触发口令**（任选一句）：`回归` · `按回归测试方案执行` · **`按照文档回归测试`**

---

## ⚠️ 给 Agent 的硬约束（读完本文第一条就要做）

1. **Layer 0 由你（Agent）在终端执行**，不是让用户执行；**禁止**对用户说「请你运行以下命令」。  
2. 读完本文后 **第一件事**：在仓库根目录执行（二选一，推荐 A）：  
   - **A（推荐，一条搞定）**：`python scripts/run_layer0.py`  
   - **B**：按 §Layer 0 四条命令 **分四次** `run_command`（不得漏 0.3 `check_agent_v3_health.py`）。  
3. Layer 0 任一步 **exit code ≠ 0** → **停止**，汇报完整终端输出，**不得**进入 Layer 1。  
4. Layer 0 全过 → 再按 §Layer 1 / Layer 2 用 tool 做冒烟与场景。  
5. **禁止**问用户「测 v2 还是 v3」；`agent_v2/` 已删除，仅使用 `agent_v3` 与 `scripts/run_layer0.py`。

---

## 用户约定（执行者必读 — 默认意图）

| 约定 | 说明 |
|------|------|
| **谁跑命令** | **Agent 自己跑**；用户只发口令「按文档回归」，不负责敲 Layer 0。 |
| **怎么测** | 用户**永远**用**本文档**做回归；说「回归 / 按文档回归」= 你先自动跑完 Layer 0，再 Layer 1 →（可选）Layer 2。 |
| **测什么** | **当前仓库最新重构后的完整运行栈**：`deepseek_code_agent3.py` → **`agent_v3/`**（`agent_core` 门面 + `agent_v3/core/*` 子模块）+ **`tools/`** + **`util/`** + catalog / 提示词。与托盘 `main_tray.py` 启动栈一致。 |
| **禁止行为** | 只列命令不执行；让用户代跑 Layer 0；使用已删除的 `agent_v2` 路径。 |
| **产品版本号** | 对外仍为 **v1.4**（`agent_v3/version.py`）；**agent_v3** 是内部工程目录名，不是旧产品代际。 |

**最后更新**：2026-05-27

---

## 自描述 · 执行流程

当用户说「按照文档回归测试」或上述口令时，执行者应 **先 `read_file` 本文件**（若上下文未包含全文），然后**严格按本节与后文各章**对 **agent_v3 全栈** 执行，**不要**向用户追问「要先跑哪一步 / 测哪一代」。

**回归测试流程：先 Layer 0 → 再分批 Layer 1 → Layer 2（可选）。**

```
Layer 0（自动化，必须先过）
    ↓ 全部通过
Layer 1（工具冒烟，按章节分批）
    ↓ 用户要求或发版前
Layer 2（场景验收，可选）
```

### 第 1 步 — Layer 0（Agent 必须自动执行）

1. **立刻**在仓库根目录执行（推荐一条命令包含全部检查）：

```powershell
python scripts/run_layer0.py
```

   内部顺序：`unittest` → `agent_v3/check_symbols.py` → `scripts/check_agent_v3_health.py` → `compileall agent_v3 tools util`。

2. 若 `run_layer0.py` 不可用，则 Agent 用 `run_command` **分四次**执行 §Layer 0 四条命令；**禁止**拼成一条含 `;` / `&&` / `|` 的命令（`safe_mode` 会拦截）；**禁止**漏跑 `check_agent_v3_health.py`。
3. 任一步失败 → **停止**，粘贴终端输出，**不进入** Layer 1。
4. 全部通过 → 汇报「Layer 0 通过（已自动执行 run_layer0 或等价四条）」，进入第 2 步。

### 第 2 步 — Layer 1（分批）

1. 确认 **§前置准备** 中 `E:\AgentTest\` 测试资源存在；缺失则先创建再继续。
2. 按 **§测试用例** 章节**分批**执行（建议每批约 5～8 个小节，如 1–8、9–16…），避免单轮 tool 次数耗尽。
3. 每条用例：按表内「操作」调用 tool → 对照「工具返回」→ 用 `read_file` / `glob_files` 等**旁路验盘** → 在汇报中将「结果」标为 ✅ 或 ❌。
4. 写操作须覆盖 Plan 拦截；标 ⏸️ 的用例（`run_command`、`python_inline`、kling/dreamina 等）若用户未授权，跳过并注明「待用户授权」。
5. 每批结束输出：**本批通过数 / 失败数 / 失败用例编号**；全部批次完成后汇总 Layer 1 结论。
6. Layer 1 存在 ❌ → 汇报失败详情，询问是否修代码后重跑；未要求则不擅自进入 Layer 2。

### 第 3 步 — Layer 2（可选）

仅在用户明确要求「继续 Layer 2」或「做场景验收」时执行，见 [`团队开发架构方案.md`](团队开发架构方案.md) 狼人杀 / 协调 Agent 流程。

### 汇报格式（建议）

```text
【回归测试】
Layer 0: 通过 / 失败（附命令输出摘要）
Layer 1: 进行中 第 x/y 批 | 已完成 — 通过 n 条，失败 m 条（列出编号）
Layer 2: 未执行 | 进行中 | 通过 / 失败
```

---

## 回归分层

| 层级 | 何时跑 | 内容 | 预期 |
|------|--------|------|------|
| **Layer 0 — 自动化** | **每次** 改 `agent_v3/`、`tools/`、`util/`、`tool_list_agent.json`、提示词 | 见下节命令（**仅 agent_v3**） | 全部通过才可进入 Layer 1 |
| **Layer 1 — 工具冒烟** | 大重构、新增/修改工具、改 Plan/Execute 拦截 | 本文 §测试用例（catalog 38 项 + 宿主行为） | 结果列填 ✅ / ❌ |
| **Layer 2 — 场景验收** | 发版前、Session Mesh 大改 | 狼人杀 10 Agent、协调 Agent 并行开发（见 [`团队开发架构方案.md`](团队开发架构方案.md)） | 端到端通过 |

**执行顺序**：见上文「自描述 · 执行流程」— Layer 0 → 分批 Layer 1 →（可选）Layer 2。

---

## Layer 0 — 自动化（必跑 · Agent 自动执行）

在**本仓库根目录**由 **Agent 用终端执行**（勿让用户手跑）。

### 一键执行（推荐）

```powershell
python scripts/run_layer0.py
```

脚本路径：`scripts/run_layer0.py`。exit code `0` = Layer 0 通过。

### 前置依赖（首次或 CI 干净环境）

```powershell
pip install -r requirements.txt
```

`github_api` 等工具 HTTP 使用标准库 `urllib`，**无需** `requests`。

### 等价四条命令（仅当无法跑 run_layer0 时拆开执行）

**0.1 单元测试**

```powershell
python -m unittest discover -s tests -v
```

**0.2 符号检查（agent_v3 门面 + routes 引用）**

```powershell
python agent_v3/check_symbols.py
```

**0.3 运行栈健康检查（拆 core 后必跑）**

```powershell
python scripts/check_agent_v3_health.py
```

覆盖：运行栈无 `import agent_v2`、无废弃 `TOOL_AGENT_V2_*`、主 system 提示词、`session_create` import、**catalog ↔ `agent_main` 参数对齐**（全工具 `--flag` 与签名一致）、**`session_*` 契约**、core `LOAD_GLOBAL` 全扫描、`turn_runner` 无循环 import 等。可单独跑 `python scripts/_verify_v3_round2.py`（与 health 子集等价）。

**0.4 语法编译**

```powershell
python -m compileall agent_v3 tools util -q
```

若用 `run_command` 代跑 Layer 0，每次只传**一条** `command`（例如仅 `python -m unittest discover -s tests -v`），不要 `Set-Location; python ...`。

### 单元测试说明

| 文件 | 覆盖 |
|------|------|
| `tests/test_prompt_tool_names.py` | catalog purpose/function 命名、action= 写法、禁止简称、agent_hints、主 system 非 v1 遗留 |
| `tests/test_core_helpers.py` | UTF-8 截断、peer 消息识别、连续 peer 限流、模型 context 窗口（**import agent_v3**） |
| `tests/test_tool_regression_fixes.py` | replace_in_file / web_fetch IDNA / github 信封 / `file_search` 宿主 gate / tool_calls 快照等（**import agent_v3**） |
| `tests/test_agent_tool_budget.py` | 单轮 tool 预算与 `ToolCallLimitReached` 信封 |
| `tests/test_session_tool_ux.py` | `session_wait` 哨兵/thread、`suspend` 门控、`should_suspend_after_session_wait` 宿主挂起条件 |
| `tests/test_catalog_param_policy.py` | 只读 schema 无 restrict/run_type；`data_table`/`glob_files` 拒绝废弃别名 |

### Layer 0 通过标准

四条命令均 **exit code 0**（当前约 **48** 项单元测试，随 `tests/` 增长）；任一失败 → **停止**，不进入 Layer 1。  
**改 catalog / 提示词 / `agent_v3` core 后，Layer 0 不过则禁止合入。**

---

## 开发规范（新增工具 / 拓展能力必遵）

### 1. 工具实现

| 项 | 要求 |
|----|------|
| 路径 | `tools/<name>.py` |
| 入口 | `agent_main(**kwargs) -> dict` |
| CLI | 支持 `argparse` + `--json_out` |
| 错误信封 | `{"ok": false, "data": null, "error": {"type": "...", "message": "..."}}` |
| 参数名 | **snake_case**，与 JSON schema 一致 |
| 写操作 | 默认 `dry_run=true`；Execute 才真实写盘 |
| Plan 模式 | 写类工具返回 `ModeConflict` |
| 单一职责 | 一工具一类问题，不堆多功能 |
| `agent_main` 签名 | catalog 中每个 `--snake_case` 参数须在 `agent_main` 显式出现（或 `**kwargs`）；由 health `check_catalog_agent_main_alignment` 校验 |

### 2. catalog 注册（`tool_list_agent.json`）

| 项 | 要求 |
|----|------|
| `name` | 对应 `tools/` 下文件名，如 `read_file.py` |
| `function` | OpenAI tools 名，snake_case，与实现一致 |
| `purpose` / `description` | **必须包含完整 function 名**；多 action 工具写 `action=check` 等（`session_*` 无 action） |
| 禁止简称 | 不用 `grep` 代 `grep_files` 等（由 `test_prompt_tool_names` 校验） |

### 3. 提示词与宿主

| 项 | 要求 |
|----|------|
| 系统提示 | 变更同步 `util/agent_prompt_constants.py` |
| Execute 写盘 | 须先 `todo_list` |
| peer 协作 | `session_send` 等 `requires_reply` 必填；P0 须 ephemeral 回复 |
| 符号引用 | 新 public 符号跑 `agent_v3/check_symbols.py` + `scripts/check_agent_v3_health.py` |

### 4. 协作工具（`session_*`）专规

| 项 | 要求 |
|----|------|
| 无 `action` | 六件套 **不得** 在 catalog / OpenAI schema / CLI 暴露 `action`；宿主 `execute_tool_script` 会剥离遗留 `action` 字段 |
| `session_wait` | `suspend=true`（或省略，默认 true）且 pending → 宿主挂起；`suspend=false` → **仅查询**，不调用 `suspend_agent_wait`，不因 `should_stop_turn` 挂起 |
| 工具 import | `tools/*.py` 顶层 `import agent_common`；工具间 `from session_send import …`；**禁止** `from tools.xxx` 与 `try/except` 双轨 import |
| 测试 import | 与宿主一致：`sys.path` 插入 `tools/` 后 `import session_wait`（勿 `from tools import session_wait`） |
| `file_search` | 仅宿主在 `set_file_search_allowed(cid)` 后、且 `execute_tool_script(..., conversation_id=cid)` 时可调用；进度线程须传 `conversation_id` |

### 5. 测试与文档

1. Layer 0 全绿  
2. 在本文 **§测试用例** 为该工具补用例（参数全覆盖）  
3. 在 [`进行中的拓展工具.md`](进行中的拓展工具.md) 从「待拓展」移到「已实现」  

### 6. 非 catalog 模块

`stdio_utf8.py`、`tool_help_share.py`、`command_safety.py` 等为**宿主内部依赖**，不在 catalog 注册，**不单独列工具用例**；`run_command` 用例已覆盖 command_safety 行为。

---

## 补全测试用例规则

1. catalog 中每个 function 至少 1 条用例；多参数须参数级覆盖。  
2. 每条用例写明输入与验收标准。  
3. 验收不能只看 `ok`：写操作用 `glob_files` / `read_file` 旁路验盘。  
4. 写操作须含 Plan 模式拦截用例。  
5. `run_command`、`python_inline` 须用户明确授权（⏸️）。  
6. 结果列填 **待测** / **✅** / **❌**，勿预填通过。

---

## 前置准备（Layer 1）

```text
E:\AgentTest\
├── src\
│   ├── hello.txt       # "Hello World"
│   ├── config.json     # {"name": "test", "version": 1}
│   └── code.py         # def foo/bar 示例
├── archive\            # 解压目标
└── images\
    └── test_blank.png  # 可选
```

OCR 用项目内 `res/img/test_ocr.png`。

---

## 测试用例

> **结果列**：执行后填 `待测` → `✅` / `❌`。下表初始均为 **待测**。

### 1. archive（5）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 1.1 | list 不存在 | `action=list` `source=E:\不存在.zip` | FileNotFoundError | 待测 |
| 1.2 | create zip | `action=create` `source=E:\AgentTest\src\hello.txt` `dest=E:\AgentTest\hello.zip` | ok + 磁盘存在 | 待测 |
| 1.3 | list zip | `action=list` `source=E:\AgentTest\hello.zip` | entries 含 hello.txt | 待测 |
| 1.4 | extract | `action=extract` `source=E:\AgentTest\hello.zip` `dest=E:\AgentTest\archive` | extractedCount>=1 | 待测 |
| 1.5 | Plan 拦截 extract | Plan 模式下 1.4 | ModeConflict | 待测 |

### 2. run_command（5）⏸️

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 2.1 | echo | `command=echo hello` | exit 0, stdout 含 hello | 待测 |
| 2.2 | cwd | `command=cd` | exit 0 | 待测 |
| 2.3 | 失败 | `command=exit 1` | exit 1 | 待测 |
| 2.4 | 黑名单 | `command=rm -rf /` | 拦截未执行 | 待测 |
| 2.5 | safeMode | `command=echo hello && dir`（含 `&`） | ValueError/safe_mode 拦截，提示禁止字符 | 待测 |

### 3. data_table（5）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 3.1 | 不存在 | `action=preview` `source=E:\不存在.csv` | FileNotFoundError | 待测 |
| 3.2 | preview CSV | 创建 test.csv 后 preview | 表头+数据 | 待测 |
| 3.3 | filter | filter_col/filter_val | 筛选结果 | 待测 |
| 3.4 | to_json | `action=to_json` | JSON 内容 | 待测 |
| 3.5 | sheets xlsx | `action=sheets` | 表名或合理错误 | 待测 |

### 4. env_probe（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 4.1 | 基本 | 无参 | platform/python/cwd | 待测 |
| 4.2 | json_out | `json_out=true` | JSON 信封 | 待测 |
| 4.3 | available | 检查 data.available | 含 cmd/python | 待测 |

### 5. file_ops（5）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 5.1 | copy dry_run | copy hello.txt → hello_copy.txt dry_run=true | 预览，磁盘无文件 | 待测 |
| 5.2 | copy 执行 | dry_run=false Execute | 目标存在 | 待测 |
| 5.3 | rename | rename hello_copy → hello_renamed | 新名存在 | 待测 |
| 5.4 | move | move 到 moved/ | 原无、目标有 | 待测 |
| 5.5 | Plan 拦截 copy | Plan + copy dry_run=false | ModeConflict | 待测 |

### 6. grep_files（4）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 6.1 | 字面 | path=src pattern=def | 命中 code.py | 待测 |
| 6.2 | 正则 | pattern=def\\w+ regex=true | 匹配函数 | 待测 |
| 6.3 | 上下文 | context_lines=1 | 含上下文 | 待测 |
| 6.4 | 无匹配 | pattern=XXXXXXXXX | match_count=0 | 待测 |

### 7. replace_in_file（6）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 7.1 | dry_run | old_text=42 new_text=0 dry_run=true | diffText，磁盘未改 | 待测 |
| 7.2 | 执行 | dry_run=false Execute | 42→0 | 待测 |
| 7.3 | rules 批量 | rules=[...] | foo/bar 替换 | 待测 |
| 7.4 | 行区间 | line_start/end | 指定行替换 | 待测 |
| 7.5 | region | region_start/end | 区间替换 | 待测 |
| 7.6 | Plan 拦截 | Plan + 7.2 | ModeConflict | 待测 |

### 8. image_ocr（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 8.1 | 不存在 | source=不存在.png | FileNotFoundError | 待测 |
| 8.2 | 有效图 | source=res/img/test_ocr.png | 识别文本 | 待测 |
| 8.3 | easyocr | engine=easyocr | 结果或引擎缺失提示 | 待测 |

### 9. ip_geolocate（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 9.1 | 本机 | 无参 | ip/city/country | 待测 |
| 9.2 | 8.8.8.8 | ip=8.8.8.8 | 合理地理信息 | 待测 |
| 9.3 | 127.0.0.1 | ip=127.0.0.1 | 查出口 IP | 待测 |

### 10. open_meteo_weather（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 10.1 | 城市 | location=北京 | current/daily | 待测 |
| 10.2 | 经纬度 | lat/lon forecast_days=1 | 天气数据 | 待测 |
| 10.3 | 无效城市 | location=不存在的城市名xxxx | NotFound | 待测 |

### 11. apply_patch（4）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 11.1 | 缺 patch | 无 patch_text/patch_file | 参数错误 | 待测 |
| 11.2 | dry_run | patch_text + dry_run=true | diff_text | 待测 |
| 11.3 | 执行 | dry_run=false Execute | 文件已更新 | 待测 |
| 11.4 | Plan 拦截 | Plan + 11.3 | ModeConflict | 待测 |

### 12. python_inline（4）⏸️

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 12.1 | print | code=print(1+1) | stdout=2 | 待测 |
| 12.2 | 写文件 | 写 py_test.txt | 磁盘存在 | 待测 |
| 12.3 | 异常 | code=1/0 | exit 1 + stderr | 待测 |
| 12.4 | Plan 拦截 | Plan + 12.1 | ModeConflict | 待测 |

### 13. regex_locate（4）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 13.1 | 匹配 | pattern=def | 函数行 | 待测 |
| 13.2 | ignore_case | pattern=DEF ignore_case=true | 命中 def | 待测 |
| 13.3 | 无匹配 | pattern=不存在 | count=0 | 待测 |
| 13.4 | 递归 | recursive glob_pattern=*.py | 多文件 | 待测 |

### 14. run_type（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 14.1 | 查询 | 无参 | run_type 字段 | 待测 |
| 14.2 | Plan | run_type=plan | 已切换 | 待测 |
| 14.3 | Execute | run_type=execute | 已切换 | 待测 |

### 15. read_file（5）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 15.1 | 全文 | hello.txt | Hello World | 待测 |
| 15.2 | 行区间 | line_start/end | 指定行 | 待测 |
| 15.3 | 字符区间 | char_start/end | Hello | 待测 |
| 15.4 | encoding=auto | hello.txt | 正常读取 | 待测 |
| 15.5 | 不存在 | 不存在路径 | FileNotFoundError | 待测 |

### 16. write_file（4）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 16.1 | dry_run | dry_run=true | diff，磁盘无文件 | 待测 |
| 16.2 | 写入 | dry_run=false Execute | 磁盘匹配 | 待测 |
| 16.3 | create_only | 已存在 + create_only | 错误 | 待测 |
| 16.4 | Plan 拦截 | Plan + 16.2 | ModeConflict | 待测 |

### 17. text_diff（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 17.1 | 相同 | left=right=abc | same=true | 待测 |
| 17.2 | 不同 | abc vs abd | same=false | 待测 |
| 17.3 | 文件 | left_file/right_file | diff 正确 | 待测 |

### 18. file_search（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 18.1 | 字面 | pattern=def | 匹配行 | 待测 |
| 18.2 | 正则 | pattern=def\\w+ regex=true | 函数名 | 待测 |
| 18.3 | 上下文 | context_lines=2 | 含上下文 | 待测 |

### 19. todo_list（5）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 19.1 | create | action=create items=[...] | list_id | 待测 |
| 19.2 | add_item | action=add_item | 项增加 | 待测 |
| 19.3 | check | action=check indices=[0] | 已勾选 | 待测 |
| 19.4 | query | action=query | 完整状态 | 待测 |
| 19.5 | close | action=close | 已关闭 | 待测 |

### 20. unified_diagnose（4）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 20.1 | 目录 | path=src limit=5 | 诊断结果 | 待测 |
| 20.2 | 语法错 | 含语法错误 .py | 发现错误 | 待测 |
| 20.3 | no_ruff | no_ruff=true | ruff_attempted=false | 待测 |
| 20.4 | 路径不存在 | 不存在目录 | NotADirectoryError | 待测 |

### 21. user_confirm（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 21.1 | 带 confirm | confirm=A | data.confirm=A | 待测 |
| 21.2 | 首次无 confirm | 无 confirm | E_USER_CONFIRM_REQUIRED | 待测 |
| 21.3 | peer 场景 | 最近 user 来自 peer cid | 不弹 UI，提示 session_send | 待测 |

### 22. web_fetch（4）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 22.1 | 无效 URL | 不存在域名 timeout=5 | 超时/错误 | 待测 |
| 22.2 | httpbin | url=https://httpbin.org/get | 有内容 | 待测 |
| 22.3 | max_chars | max_chars=50 | truncated=true | 待测 |
| 22.4 | 空 URL | url= | 参数错误 | 待测 |
| 22.5 | 中文域名 | url=http://例子.测试/ timeout=5 | 不报 latin-1 编码错；可为网络/超时错误 | 待测 |

### 23. find_in_file（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 23.1 | 字面 | pattern=def | region_start/end | 待测 |
| 23.2 | occurrence | occurrence=1 | 第 2 个 def | 待测 |
| 23.3 | 无匹配 | pattern=XXXXXXXX | found=false | 待测 |

### 24. delete_file（4）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 24.1 | dry_run | dry_run=true | would_move_to | 待测 |
| 24.2 | 删除 | dry_run=false Execute | 原文件不存在 | 待测 |
| 24.3 | 不存在 | 不存在路径 | FileNotFoundError | 待测 |
| 24.4 | Plan 拦截 | Plan + 24.2 | ModeConflict | 待测 |

### 25. glob_files（4）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 25.1 | 全部 | glob_pattern=* | 文件列表 | 待测 |
| 25.2 | *.txt | glob_pattern=*.txt | 仅 txt | 待测 |
| 25.3 | 递归 | **/* recursive=true | 递归列表 | 待测 |
| 25.4 | 不存在 | 不存在目录 | 路径错误 | 待测 |

### 26. read_write（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 26.1 | dry_run | hello→rw_copy dry_run=true | 预览无文件 | 待测 |
| 26.2 | 复制 | dry_run=false Execute | 内容一致 | 待测 |
| 26.3 | 行区间 | line_start/end | 仅指定行 | 待测 |

### 27. git_workspace（2）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 27.1 | 非 Git | path=E:\AgentTest mode=log | 非仓库错误 | 待测 |
| 27.2 | 正常仓库 | path=项目根 mode=log log_max=5 | 提交记录 | 待测 |

### 28. web_fetch_render（2）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 28.1 | 有效页 | url=https://httpbin.org/html | 可读正文 | 待测 |
| 28.2 | 超时 | 无效域名 timeout 短 | 错误 | 待测 |

### 29. skill_manage（2）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 29.1 | list | action=list | 返回 skill 列表 | 待测 |
| 29.2 | load | action=load name=已知 skill | 加载成功或合理错误 | 待测 |

### 30. github_api（2）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 30.1 | 无 token 公开读 | action 读公开 repo（按 schema） | 合理响应或鉴权提示 | 待测 |
| 30.2 | 无效 action | 缺必填参数 | 参数错误 | 待测 |

### 31. kling_generate（2）⏸️

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 31.1 | 缺密钥 | 无有效 API 配置 | 明确配置错误 | 待测 |
| 31.2 | 最小请求 | 有效配置 + 最小 prompt（用户授权） | task_id 或异步状态 | 待测 |

### 32. dreamina_generate（2）⏸️

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 32.1 | 缺密钥 | 无配置 | 配置错误 | 待测 |
| 32.2 | 最小请求 | 有效配置（用户授权） | 任务/结果字段 | 待测 |

### 33. session_create（2）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 33.1 | 批量创建 | count=2 name_prefix=测试 | 返回 cid 列表 | 待测 |
| 33.2 | session_list 可见 | 创建后 list | 含新 Agent | 待测 |

### 34. session_send（4）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 34.1 | 缺 requires_reply | 无 requires_reply | 参数错误 | 待测 |
| 34.2 | 点对点 | target_id + requires_reply=false | 发送成功 | 待测 |
| 34.3 | requires_reply=true | 目标 Agent 收到 + P0 ephemeral | 须 session 回复 | 待测 |
| 34.4 | 目标忙碌 | 目标 run 中发送 | 入 inbox 队列 | 待测 |

### 35. session_multisend / session_broadcast（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 35.1 | multisend | target_ids 2 个 + message | 双目标收到 | 待测 |
| 35.2 | broadcast 无 tag | 空 tag | 拒绝或参数错误 | 待测 |
| 35.3 | broadcast 按 tag | tag=测试标签 | 仅匹配 Agent 收到 | 待测 |

### 36. session_wait（3）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 36.1 | 仅检查 | suspend=false，thread_id=... | pending，`suspend=false`，宿主 **不** 挂起 | 待测 |
| 36.2 | 挂起等待 | suspend=true（或省略）未回复 | 当前 run 结束并 wait_state | 待测 |
| 36.3 | 回复后恢复 | 目标 session_send 回写后 | 发送方自动续跑 | 待测 |

### 37. session_list（1）

| # | 测试 | 操作 | 工具返回 | 结果 |
|---|------|------|----------|------|
| 37.1 | 列表字段 | 无参 | cid/name/status/tags/queueSize | 待测 |

### 38. 宿主行为（Layer 1 补充）

| # | 测试 | 操作 | 验收 | 结果 |
|---|------|------|------|------|
| 38.1 | context_layout | 发一条消息后看 UI 用量条 | 七段中文 label + 剩余容量 | 待测 |
| 38.2 | Execute 无 todo | Execute 下直接 write_file | 拦截提示先 todo_list | 待测 |
| 38.3 | peer 限流 | 连续 peer 超 max_consecutive_peer_turns | 宿主拒绝或降级 | 待测 |
| 38.4 | SSE 多 tab | 两 Agent 同时跑 | `/api/events/stream` 按 cid 分发 | 待测 |
| 38.5 | file_search gate | 无 gate 调 file_search | Restricted | 待测 |
| 38.6 | session_wait 挂起 | suspend=false + pending | 不触发 `agent_wait_suspended` | 待测 |

---

## Layer 2 场景验收（规范）

> 用于「谁是卧底」/「狼人杀」等多人 Agent 协作场景回归。以下规范执行裁判（当前 Agent）**必须遵守**，避免因裁判设计疏忽导致场景失效。

### Layer 2 裁判规则（必读）

> ⚠️ 以下规则为历次游戏沉淀的完整约束，执行裁判必须逐条遵守。

#### 一、通信规则

1. **广播必须包含裁判自己**
   - `session_multisend` / `session_broadcast` 的 target 必须包含裁判的 CID，否则裁判收不到玩家发言。
   - 裁判 CID 从 `session_create` 返回的 `created_by` 字段获取。

2. **完整 CID 周知（禁止扫文件）**
   - 每轮开始前，裁判必须用 `session_multisend` 向全体玩家广播**完整 CID + 存活状态表**，格式：
     ```
     玩家1(full-cid-here) 存活
     玩家2(full-cid-here) 出局
     ```
   - 禁止缩写 CID（必须用完整 UUID 串）。玩家不得到 `agent_sessions.json` 中扫盘查 CID。

3. **白天所有行动公开**
   - 发言：`session_multisend` 广播给所有存活玩家+裁判
   - **投票：`session_multisend` 广播给所有存活玩家+裁判**（禁止私信投票）
   - 白天无任何私密环节。

#### 二、秩序控制

4. **todolist 跟踪执行**
   - 每一天/每一夜开始前，裁判先用 `todo_list(action=create)` 创建当日步骤清单。
   - 每完成一步立即 `todo_list(action=check, indices=[n])` 勾选。
   - 全部完成后 `todo_list(action=close)`。
   - 示例清单：「点名玩家1→点名玩家2→…→投票→公布结果」

5. **串行点名（绝对禁止并行）**
   - 裁判按顺序（1→2→3…）一次点名一位玩家。
   - 等该玩家发言广播到达后，再点名下一位。
   - 未被点名的玩家禁声。违规发言 → 裁判警告矫正。

6. **发言来源检查**
   - 每条发言必须验证来源是否为 `channel=group`（即 `session_multisend`）。
   - 若玩家用错方式（如 `session_send` 私信），**立即矫正要求重新广播**。
   - 若用错方式导致身份底牌泄漏 → **游戏终止重开**。

#### 三、夜晚行动

7. **夜晚行动顺序（严格）**
   - 预言家 → 守卫 → 狼人 → 女巫 → 猎人
   - 上一步完成后再执行下一步，不可并行。

8. **狼人夜间约束**
   - 用 `session_multisend` 拉「狼人+裁判」局部广播群。
   - 消息用 **`requires_reply=false`**，不要求逐条回复。
   - 设定 **60 秒投票截止**，格式：「当前时间 XXXX，60 秒内投票截止，票多者杀」。
   - 过期未投票视为弃权。
   - 沟通范围仅限于狼人群，不得广播到全体。
   - ⚠️ **发错频道自爆**：狼人若将内部讨论发到公共频道（target_ids 包含非狼玩家），视为自动暴露身份，裁判不作提醒，后果自负。

8b. **狼人绑票规则（裁判必知）**
    - 每轮白天结束后，裁判立即检查绑票条件。
    - **绑票条件（白天判断）**：
      
      ① **狼人数 > 好人人数** → 绑票 ✅（白天投票狼必胜）
      
      ② **狼人数 = 好人人数** → 分情况：
         - 女巫**双药在手**（解药+毒药都有）→ ❌ 不绑票（夜可救一民+毒一狼，追回两轮次）
         - 女巫**只有毒药** → ✅ **绑票**（毒一狼改变不了白天人数相等格局。最终狼刀女巫+女巫毒狼，双方死，好人全军覆没，判狼赢）
      
    - 满足绑票条件时 → 游戏**立即结束**，狼人获胜。裁判无需进入下一轮流程。
    - 若裁判忘记宣告，狼人玩家可主动提醒裁判。
    - ⚠️ **裁判继续流程的后果**：若绑票条件已满足但裁判错误地继续游戏，导致狼人后续被淘汰，**结果仍以绑票判定为准**——狼人获胜。裁判失误不改变游戏结果。
    - 示例：
      - 2狼 vs 1好人 → 绑票 ✅（狼人多）
      - 1狼 vs 1好人，女巫无毒 → 绑票 ✅（人数相等且女巫无药）
      - 1狼 vs 1好人，女巫有毒 → 绑票 ✅（双方死，好人全军覆没）
      - 1狼 vs 1好人，女巫双药 → 不绑票 ❌
      - 2狼 vs 2好人，女巫只有毒药 → 绑票 ✅
      - 2狼 vs 2好人，女巫双药 → 不绑票 ❌

9. **女巫/预言家/守卫/猎人**
   - 裁判私信通知 → 玩家用 `session_send` 私回裁判（不广播）。
   - 猎人被淘汰时，裁判私信确认是否开枪。开枪也用 `session_send` 私回。

#### 四、投票与出局

10. **投票公开化**
   - 投票指令：`session_multisend` 广播。
   - 投票执行：玩家用 `session_multisend` 公开广播投票（不再私信裁判）。
   - 裁判统计后广播结果。

11. **平票PK机制**
    - 平票时，平票玩家依次重新点名发言一轮。
    - 台下存活玩家对PK台玩家重新投票。
    - 若仍平票，裁判按点名顺序强行归票。

12. **不透露底牌**
    - 公布淘汰结果时 **不揭露被淘汰者的身份角色**。
    - 仅宣布「玩家X号被投票出局/被淘汰」。

#### 五、裁判行为禁区

13. **裁判不教玩家玩游戏**
    - 裁判只做：传递信息、点名、统计、公布结果。
    - 禁止添加任何策略建议（如「请引导大家」「注意隐晦表达」「建议你…」）。

14. **轮到谁就是谁**
    - 游戏流程中不催促、不跳步、不并行。

#### 六、Agent 异步工作方式（裁判必读）

> ⚠️ **以下规则为历次狼人杀游戏实践中裁判（Agent）暴露出的核心问题总结，执行裁判必须逐条理解并遵守。**

15. **发消息即停，等人回复叫醒**
    - 裁判发完一条 `session_send`（`requires_reply=true`）后，**立即停止调工具，结束当前对话回合**。
    - **禁止**主动轮询（`read_file` 读 inbox、`session_list` 查状态、`session_wait` 等）。对方的回复会由系统自动唤醒下一轮对话。
    - 示例：点名玩家发言 → `session_send` 发出 → **停** → 玩家回复唤醒 → 点名下一位。

16. **绝对串行点名**
    - 每次只点名一位玩家，等该玩家发言广播到达并处理后，再点名下一位。
    - **禁止**一次点名多人（如一次发 5 条 `session_send` 给多名玩家）。

17. **收齐再统计，禁止提前宣布**
    - 投票阶段：发出投票通知后**停**，等所有玩家的投票都到达后，再一次性统计并公布结果。
    - **禁止**票没齐就宣布结果、禁止催促未投票玩家（系统异步，票会陆续到）。
    - **禁止**二次更正公告：统计确认无误后再发，发即终版。

18. **不焦虑预算，允许跨回合**
    - 工具调用次数（tool_calls_limit）用完就正常结束对话回合，等下一轮继续。
    - **禁止**为了省次数而批量操作（一次叫多人、不等齐就统计等）。
    - 一局游戏可以跨几十甚至上百个对话回合完成，这是正常现象。

19. **不伪造游戏事件**
    - 裁判的职责是传递玩家决定、统计并公布。**禁止**代替玩家做任何决定（如宣布毒杀了没用毒的目标、宣布没人刀死的死亡等）。

20. **只私信纠正，不群发改正公告**
    - 若发现发错对象等操作失误，立即私下 `session_send` 向对应玩家道歉并纠正。
    - **禁止**发全员更正公告——这会干扰所有玩家的判断。

21. **不使用 `session_wait`**
    - `session_wait` 的设计与本异步工作模式冲突。发 `requires_reply=true` 的消息后，系统会自动在对方回复时唤醒你。
    - 不需要主动等待、不需要检查是否已回复。相信系统会叫你。

22. **统一 thread_id 命名，禁止碎片化**
    - 裁判在每晚/每天开始时决定该轮次的 thread_id，并在第一条消息中固定使用，后续所有相关消息复用同一 thread_id。
    - 命名规则：`{轮次}-{环节}`，例如：
      - 第一夜狼人讨论 → `n1-wolf`
      - 第二夜狼人讨论 → `n2-wolf`
      - 第一天发言 → `d1-speak`
      - 第一天投票 → `d1-vote`
      - 第一天PK → `d1-pk`
    - **禁止**多人使用不同 thread_id 讨论同一话题。
    - 代码层面：`session_send` / `session_multisend` 的 `thread_id` 传空时会自动生成 `auto-{发送方CID前8位}-{时间戳}`。裁判应尽量手动指定有意义的 thread_id（如 `n1-wolf`），方便消息归组和排查。

#### 七、游戏善后

23. **游戏结束通知**
    - 公布结果后，通知所有玩家游戏结束，感谢参与。

24. **规则变更沉淀**
    - 任何新增/修改的规则必须同步更新本文档的「裁判规则」和「验收流程」。

### Layer 2 验收流程

1. **开局**：`session_create` 创建 N 个玩家（带完整 Persona + tags），Persona 中必须包含通信规则和违规后果。
2. **身份分配**：`session_send` × N 私发身份（`requires_reply=true`），附带**完整 CID 总表**。
3. **每轮开始**：`session_multisend` 广播**完整 CID + 存活状态表**。
4. **夜晚行动（严格串行）**：
   - 预言家：`session_send` 私信 → `session_send` 私回
   - 守卫：`session_send` 私信 → `session_send` 私回
   - 狼人：`session_multisend` 局部广播（狼人+裁判，`requires_reply=false`，60s截止）→ 群内投票
   - 女巫：`session_send` 私信 → `session_send` 私回
   - 猎人（若被刀）：`session_send` 私信 → `session_send` 私回
5. **白天发言（串行点名）**：
   - 按顺序点名：`session_send` 点名 → 玩家 `session_multisend` 广播（含裁判 CID）
   - 检查发言来源是否为 `channel=group`。用错则矫正重发。
   - 未点名玩家禁声。
6. **投票（公开广播）**：
   - 投票通知：`session_multisend` 广播
   - 玩家投票：`session_multisend` 公开广播投票目标（禁止私信）
   - 平票则 PK 台（平票者重新发言 → 台下重新投票 → 仍平则裁判归票）
7. **结果公布**：
   - 裁判统计后 `session_multisend` 广播结果
   - **不揭露出局者身份**
8. **游戏结束**：通知所有玩家，感谢参与。

---

| 用例 | 场景 | 输入 | 期望 | 实际 | 旁路检测 | 结果 |
|:----:|:------|:-----|:-----|:-----|:---------|:----:|
| 11.2 | dry_run | patch + dry_run=true | diff，磁盘不变 | … | read_file 未改 | ✅ |
| 11.4 | Plan | Plan + apply_patch | ModeConflict | … | read_file 未改 | ✅ |

---

## 通过标准

- **✅**：行为符合预期（含预期报错、`ok=false`）
- **❌**：与预期不符，或工具返回与旁路检测矛盾
- **⏸️**：需用户授权后执行

---

## 清理

测试完成后删除或回收 `E:\AgentTest` 临时文件（`delete_file` / `file_ops` 或资源管理器）。
