# 进行中的拓展工具

> 更新时间：2026-05-16 | 基于 Code Web Agent v1.2 工具库
> 已实现的工具（archive / file_search / image_ocr / data_table / kling_generate）已移入主库，不再列于此文档。

---

## 🥇 第一梯队（高频刚需 — 优先实现）

---

### 1. `pdf` — PDF 处理

| 项目 | 内容 |
|------|------|
| **依赖** | `PyMuPDF`（fitz）或 `pdfplumber` |
| **难度** | ⭐⭐ |

**功能**：
- 提取全部文本（分页）
- 提取指定页码范围
- 提取表格数据
- 合并多个 PDF
- 拆分 PDF 为单页文件
- 获取元信息（页数、作者、标题）

**典型场景**：
- 读 PDF 合同 → `提取 contract.pdf 的文本内容`
- 提取表格 → `把 report.pdf 第3页的表格导出为 CSV`
- 合并发票 → `合并 invoice1.pdf invoice2.pdf 为 all_invoices.pdf`

**参数设计**：
```json
{ "action": "extract_text", "source": "report.pdf", "page_start": 1, "page_end": 10 }
{ "action": "merge", "source": ["a.pdf","b.pdf"], "dest": "merged.pdf" }
{ "action": "split", "source": "book.pdf", "page_size": 10, "dest_dir": "pages/" }
```

---

### 2. `db_query` — 数据库通用查询

| 项目 | 内容 |
|------|------|
| **依赖** | `sqlite3`（内置）+ 可选 `pymysql`、`psycopg2` |
| **难度** | ⭐⭐⭐ |

**功能**：
- 连接 SQLite / MySQL / PostgreSQL
- 执行 SELECT 查询，结果表格化
- 列出所有表名与表结构
- 参数化查询防注入
- 结果导出 JSON / CSV
- 行数上限防大查询

**典型场景**：
- `列出 db.sqlite3 的所有表`
- `查询 users 表前10条`
- `SELECT COUNT(*) FROM orders WHERE status='pending'`

**参数设计**：
```json
{ "db_type": "sqlite", "path": "data.db", "query": "SELECT * FROM users LIMIT 10" }
{ "db_type": "mysql", "host": "localhost", "port": 3306, "user": "root", "password": "***", "database": "test", "query": "SHOW TABLES" }
```

---

### 3. `sys_info` — 系统信息监控

| 项目 | 内容 |
|------|------|
| **依赖** | `psutil` |
| **难度** | ⭐ |

**功能**：
- CPU：型号、核心数、使用率
- 内存：总量/已用/可用
- 磁盘：各分区总量/已用/可用
- 进程：按内存/CPU 排序 Top N
- 网络：网卡 IP、流量统计
- 系统：OS 版本、开机时长

**典型场景**：
- `当前 CPU 和内存占用多少？`
- `D 盘还剩多少空间？`
- `哪个进程占用内存最多？`

**参数设计**：
```json
{ "category": "cpu" }
{ "category": "memory" }
{ "category": "disk", "mount": "C:" }
{ "category": "process", "top": 5, "sort_by": "memory" }
```

---

### 4. `format_code` — 代码格式化

| 项目 | 内容 |
|------|------|
| **依赖** | `ruff` 或 `black`、`prettier`（可选） |
| **难度** | ⭐ |

**功能**：
- 格式化 Python 文件（ruff / black）
- 支持 JavaScript / TypeScript / CSS / HTML（prettier）
- 支持按行区间部分格式化
- 支持 --check 模式仅检查不修改
- 支持配置文件自动发现（pyproject.toml、.prettierrc）

**典型场景**：
- `格式化 src/app.py`
- `检查整个项目代码风格是否一致 --check`
- `用 ruff 格式化当前目录所有 .py 文件`

**参数设计**：
```json
{ "path": "src/app.py", "formatter": "ruff", "check": false, "dry_run": true }
{ "path": "src/", "glob_pattern": "*.py", "formatter": "ruff", "check": true }
{ "path": "frontend/", "glob_pattern": "*.js", "formatter": "prettier" }
```

---

### 5. `git_write` — Git 写操作

| 项目 | 内容 |
|------|------|
| **依赖** | Git CLI（系统安装） |
| **难度** | ⭐⭐ |

**功能**：
- git add / commit / push / pull
- git checkout / switch 切换分支
- git merge / rebase
- git stash / stash pop
- git tag / tag push
- git reset / revert（安全保护，默认 dry_run）

**典型场景**：
- `提交所有改动到当前分支，message: "fix: 登录bug"`
- `创建并切换到 feature/new-feat 分支`
- `推送到远程 origin`

**参数设计**：
```json
{ "action": "commit", "message": "fix: 修复登录bug", "add_all": true, "dry_run": true }
{ "action": "push", "remote": "origin", "branch": "main" }
{ "action": "checkout", "branch": "feature/new", "create": true }
{ "action": "pull", "rebase": true }
```

---

### 6. `api_test` — HTTP API 测试

| 项目 | 内容 |
|------|------|
| **依赖** | `httpx` 或 `requests` |
| **难度** | ⭐⭐ |

**功能**：
- 支持 GET / POST / PUT / DELETE / PATCH / HEAD
- 自定义请求头、查询参数、请求体（JSON / 表单 / 文本）
- 携带 Cookie 与 Bearer Token 认证
- 查看响应状态码、响应头、响应体
- 超时控制、重定向跟随
- 请求/响应历史记录
- 保存与复用请求模板

**典型场景**：
- `POST https://api.example.com/login 发送 {"user":"admin","pass":"***"}`
- `GET /api/users 带 Bearer Token 查看返回`
- `测试本地 /health 端点是否返回 200`

**参数设计**：
```json
{ "method": "GET", "url": "https://api.example.com/health" }
{ "method": "POST", "url": "https://api.example.com/login", "headers": {"Content-Type": "application/json"}, "body": {"user": "admin"}, "timeout": 30 }
{ "method": "GET", "url": "https://api.example.com/users", "headers": {"Authorization": "Bearer eyJ..."} }
```

---

## 🥈 第二梯队（效率提升）

---

### 7. `codec` — 编码/加密工具箱

| 项目 | 内容 |
|------|------|
| **依赖** | Python 标准库 |
| **难度** | ⭐ |

**功能**：
- Base64 / Base32 / Base16 编解码
- URL 编解码
- Hex ↔ 文本
- MD5 / SHA1 / SHA256 哈希
- HTML 实体编解码
- JSON 转义/反转义
- Unicode 转义/反转义

**参数设计**：
```json
{ "action": "base64_encode", "text": "hello" }
{ "action": "base64_decode", "text": "aGVsbG8=" }
{ "action": "sha256", "text": "hello" }
{ "action": "url_encode", "text": "https://example.com/a b" }
```

---

### 8. `network_diag` — 网络诊断

| 项目 | 内容 |
|------|------|
| **依赖** | 标准库 + `ping3` |
| **难度** | ⭐⭐ |

**功能**：
- Ping 检测（延迟、丢包）
- DNS 解析
- Traceroute 路由追踪
- 端口连通性检测

**参数设计**：
```json
{ "action": "ping", "target": "8.8.8.8", "count": 4 }
{ "action": "dns", "domain": "example.com" }
{ "action": "port", "host": "example.com", "port": 443 }
```

---

### 9. `file_dedupe` — 重复文件查找

| 项目 | 内容 |
|------|------|
| **依赖** | 标准库 |
| **难度** | ⭐⭐ |

**功能**：
- 按 SHA256 哈希查找重复文件
- 按大小+文件名快速预筛
- 输出重复文件组列表
- 可选删除/移动重复项（dry_run 保护）

**参数设计**：
```json
{ "path": "D:/downloads", "strategy": "hash", "dry_run": true }
{ "path": "D:/photos", "action": "delete_duplicates", "dry_run": false }
```

---

### 10. `rename_symbol` — 跨文件符号重命名

| 项目 | 内容 |
|------|------|
| **依赖** | 标准库（grep + replace） |
| **难度** | ⭐⭐⭐ |

**功能**：
- 跨文件重命名变量、函数、类名
- 支持按文件类型过滤（仅 .py / .ts / .js 等）
- 支持正则匹配模式
- 预览受影响文件列表（dry_run 默认）
- 自动跳过二进制文件与 .gitignore 目录

**典型场景**：
- `把项目中所有 old_api_call 改为 new_api_call`
- `将工具库中类名 OldHelper 重命名为 NewHelper`
- `预览重命名会影响哪些文件`

**参数设计**：
```json
{ "old_name": "old_api_call", "new_name": "new_api_call", "path": ".", "glob_pattern": "*.py", "dry_run": true }
{ "old_name": "OldHelper", "new_name": "NewHelper", "path": "src", "dry_run": false }
```

---

### 11. `pkg_manager` — 包管理器

| 项目 | 内容 |
|------|------|
| **依赖** | pip / npm（系统安装） |
| **难度** | ⭐⭐ |

**功能**：
- Python：pip install / uninstall / list / freeze
- Node：npm install / uninstall / list / outdated
- 支持 requirements.txt / package.json 自动识别
- 查看已安装包及版本
- 检查包是否有可用更新
- 安全预览（dry_run 查看变更）

**典型场景**：
- `安装 requests 包`
- `列出当前项目所有 Python 依赖`
- `检查 npm 包是否有更新`
- `卸载不需要的包`

**参数设计**：
```json
{ "manager": "pip", "action": "install", "packages": ["requests", "flask"], "dry_run": true }
{ "manager": "pip", "action": "list" }
{ "manager": "npm", "action": "install", "packages": ["axios"], "save": true }
{ "manager": "npm", "action": "outdated" }
```

---

### 12. `structured_editor` — 结构化配置编辑

| 项目 | 内容 |
|------|------|
| **依赖** | 标准库（json / yaml / configparser） |
| **难度** | ⭐⭐ |

**功能**：
- 直接按路径读取/修改 JSON / YAML / TOML / INI 文件
- 支持点分路径访问嵌套字段（如 `server.port`）
- 新增、修改、删除字段
- 格式化/压缩输出
- 语法校验
- 默认 dry_run 预览 diff

**典型场景**：
- `把 config.json 的 server.port 改为 8080`
- `向 package.json 添加一个新依赖`
- `删除 config.yml 中过时的配置项`
- `校验 settings.toml 语法是否正确`

**参数设计**：
```json
{ "action": "read", "path": "config.json", "key": "server.port" }
{ "action": "set", "path": "config.json", "key": "server.port", "value": 8080, "dry_run": true }
{ "action": "delete", "path": "config.yml", "key": "deprecated_field", "dry_run": false }
{ "action": "validate", "path": "settings.toml" }
{ "action": "format", "path": "package.json", "indent": 2 }
```

---

### 13. `convert_file` — 文件格式转换

| 项目 | 内容 |
|------|------|
| **依赖** | `pandas`（可选，用于表格转换）、`Pillow`（图片） |
| **难度** | ⭐⭐ |

**功能**：
- JSON ↔ YAML 互转
- CSV ↔ JSON 互转
- CSV ↔ Excel (xlsx) 互转
- Markdown → HTML 转换
- 文本编码转换（GBK ↔ UTF-8 等）
- 行尾转换（CRLF ↔ LF）

**典型场景**：
- `把 data.csv 转为 data.json`
- `转换 config.yml 为 config.json`
- `把 readme.md 导出为 readme.html`
- `将 GBK 编码的文件转为 UTF-8`

**参数设计**：
```json
{ "from": "data.csv", "to": "data.json", "from_format": "csv", "to_format": "json" }
{ "from": "config.yml", "to": "config.json", "from_format": "yaml", "to_format": "json" }
{ "from": "readme.md", "to": "readme.html", "from_format": "markdown", "to_format": "html" }
{ "from": "gbk.txt", "to": "utf8.txt", "from_encoding": "gbk", "to_encoding": "utf-8" }
```

---

### 14. `img_tool` — 图片生成与分析

| 项目 | 内容 |
|------|------|
| **依赖** | `Pillow`、`openai`（可选，用于 AI 生图） |
| **难度** | ⭐⭐⭐ |

**功能**：
- 获取图片基本信息（尺寸、格式、模式、文件大小）
- 格式转换（PNG ↔ JPG ↔ WEBP ↔ BMP）
- 缩放/裁剪/旋转
- 添加文字水印
- 生成简单占位图
- （可选）调用 DALL·E / Stable Diffusion API 生成图片

**典型场景**：
- `查看 logo.png 的信息`
- `把 photo.bmp 转为 photo.jpg 并压缩到 80% 质量`
- `将图片缩放到 800x600`
- `生成一张 400x300 的占位图`

**参数设计**：
```json
{ "action": "info", "path": "image.png" }
{ "action": "convert", "path": "input.bmp", "dest": "output.jpg", "quality": 80 }
{ "action": "resize", "path": "large.png", "dest": "small.png", "width": 800, "height": 600 }
{ "action": "generate", "dest": "placeholder.png", "width": 400, "height": 300, "color": "#3498db" }
```

---

### 15. `email` — 邮件发送

| 项目 | 内容 |
|------|------|
| **依赖** | 标准库（smtplib / email） |
| **难度** | ⭐⭐ |

**功能**：
- 发送纯文本邮件
- 发送 HTML 邮件
- 添加附件（文件路径）
- 支持 SMTP_SSL / STARTTLS
- 支持多收件人、抄送、密送
- 配置可持久化（配置文件或会话参数）

**典型场景**：
- `发送邮件到 user@example.com，主题"报告"，正文"请查收"`
- `发送带附件的邮件，附件为 report.pdf`
- `群发通知到多个收件人`

**参数设计**：
```json
{ "to": ["user@example.com"], "subject": "周报", "body": "请查收周报", "smtp_server": "smtp.qq.com", "smtp_port": 465, "user": "sender@qq.com", "password": "***" }
{ "to": ["a@example.com", "b@example.com"], "subject": "通知", "body": "<h1>通知</h1>", "html": true, "attachments": ["report.pdf"] }
```

---

## 🥉 第三梯队（锦上添花）

---

| # | 工具 | 功能 | 依赖 |
|---|------|------|------|
| 16 | `batch_rename` | 批量重命名（正则+模板） | 标准库 |
| 17 | `markdown_convert` | MD → HTML/PDF/DOCX | `markdown` + `weasyprint` |
| 18 | `clipboard` | 读写系统剪贴板 | `pyperclip` |
| 19 | `screenshot` | 截屏保存到文件 | `Pillow` + `mss` |
| 20 | `diff_dir` | 目录对比（文件差异清单） | 标准库 `filecmp` |
| 21 | `qrcode` | 生成/解析二维码图片 | `qrcode` + `pyzbar` |
| 22 | `translate` | 文本翻译（本地/API） | `translate` 或 API |
| 23 | `timer_cron` | 定时提醒/简单定时任务 | 标准库 |
| 24 | `process_manage` | 查看进程列表、终止进程、按 CPU/内存排序 | `psutil` |
| 25 | `env_edit` | 查看/添加/修改/删除环境变量（会话级和系统级） | 标准库 |
| 26 | `embedding_search` | 本地文件语义搜索（基于 Embedding 向量相似度） | `sentence-transformers` 或 API |
| 27 | `websocket_client` | WebSocket 连接测试、发送与接收消息 | `websockets` |

---

## 🔧 开发规范

1. 文件位置：`tools/<name>.py`
2. 注册到 `tools/tool_list_agent.json` 的 `tools` 列表
3. 参数使用 **snake_case** 命名，与现有工具一致
4. 工具入口函数统一为 `agent_main(**kwargs) -> dict`
5. 写操作默认 `dry_run=true`，Plan 模式拦截
6. 必须支持 `argparse` 解析（CLI 调试入口）和 `--json_out`
7. 错误信息返回 `{"ok": false, "data": null, "error": {"type": "...", "message": "..."}}`
8. 功能单一聚焦，不堆砌（一个工具解决一类问题）
