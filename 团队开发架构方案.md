# Agent Session Mesh 多 Agent 协作架构

> **状态**：已落地于 **`agent_v4`**（当前运行栈）  
> **入口**：经典页 `/`（主）；沉浸页 `/immersive`  
> **回归**：[`回归测试方案.md`](回归测试方案.md) Layer 2（谁是卧底等）  
> **updated**：2026-05-27

---

## 一、目标

支撑真实多 Agent 并行协作：开发分工、代码审查、测试验收、协调调度。狼人杀是验收场景——10 个 Agent 稳定完成一局，即说明点对点、组播、广播、顺序控制、并发运行与可视化基本达标。

---

## 二、核心模型

一个会话（`conversation_id`）就是一个 Agent：

```text
Agent = conversation_id + name + role + persona + tags + inbox + run_state
```

原则：

- Agent 间消息仍用 OpenAI 兼容的 `role="user"`，**不**引入 `role=cid`，避免破坏摘要、远期折叠与 tool 链。
- 身份与协作元数据走私有字段 + 正文前缀。

消息示例（字段均为 **snake_case**）：

```json
{
  "role": "user",
  "content": "[from=玩家1 | sender_cid=uuid-a | thread_id=day1-speak-1 | channel=direct | requires_reply=true]\n请发言",
  "_sender": "uuid-a",
  "_sender_name": "玩家1",
  "_sender_role": "狼人杀玩家",
  "_agent_peer_message": true,
  "_message_kind": "peer",
  "_channel": "direct",
  "_thread_id": "day1-speak-1",
  "_requires_reply": true
}
```

Boss（人类）消息无前缀或 `_sender_role=boss`；peer 消息参与摘要、远期折叠与近期窗口，与 user 消息同等处理。

---

## 三、持久化与运行时

### 3.1 Agent Registry

运行时：`agent_v4/live_state.py` → `AGENT_SESSIONS`  
落盘：`{runtime_data_root}/agent_sessions.json`

### 3.2 持久化 Inbox

目标 Agent **忙碌**时，消息写入：

```text
{runtime_data_root}/agent_inbox/{cid}.jsonl
```

当前 run 结束后自动 dequeue 并启动下一轮；发送方无需重复发送。

### 3.3 Peer 限流

`config.ini` `[agent]`：

- `max_consecutive_peer_turns` — 连续 peer 触发上限（0=不限）
- `min_peer_turn_interval_sec` — 两次 peer turn 最小间隔

---

## 四、协作工具（catalog function 名）

所有工具参数使用 **snake_case**。多 action 工具（如 `todo_list`、`file_ops`）在 purpose 中写 `action=check` 等形式；**协作六件套无 `action` 参数**。完整 schema 见 `tools/tool_list_agent.json`。

| function | 用途 |
|----------|------|
| `session_create` | 批量创建自由 Agent（name/persona/tags/count） |
| `session_send` | 点对点；**无 action 参数**；**`requires_reply` 必填** |
| `session_multisend` | 小组同播；`target_ids` + `requires_reply` |
| `session_broadcast` | 按 **`tag` 必填** 筛选广播；禁止空 tag 全员广播 |
| `session_list` | 列出 Mesh 内 Agent（cid/name/role/status/tags/queueSize） |
| `session_wait` | **无 action**；pending 时 `suspend=true` 挂起，`suspend=false` 仅查询 |

### 4.1 `session_send` 示例

```json
{
  "target_id": "uuid-b",
  "thread_id": "day1-speak-1",
  "channel": "direct",
  "requires_reply": true,
  "message": "请按顺序发言。"
}
```

### 4.2 `session_wait` 挂起与仅查询

| `suspend` | pending 时行为 |
|-----------|----------------|
| `true` 或省略（默认） | 调用 `suspend_agent_wait`；宿主结束当前 run（`agent_wait_suspended`），目标 `session_send` 回写后自动续跑 |
| `false` | 返回 pending 与 `should_stop_turn`（提示勿同轮轮询），**不**挂起、**不**写入 `AGENT_WAITS` |

工具返回里的 `should_stop_turn` 仅约束模型行为；宿主是否挂起只看 `data.suspend`（`live_state.should_suspend_after_session_wait`）。

### 4.3 工具加载与 import 约定

宿主 `agent_v4/bootstrap.py` 将 `tools/` 加入 `sys.path`，以 **顶层模块名** 加载（如 `import session_send`）。因此：

- `tools/session_*.py` 使用 `import agent_common as ac`，工具间 `from session_send import agent_main`
- **禁止** `from tools.xxx import …` 与 `try/except ImportError` 双轨 import
- 单元测试须模拟宿主：先 `sys.path.insert(0, tools_dir)` 再 `import session_wait`

### 4.4 P0：`requires_reply` 协作优先级

入站 peer 消息 `requires_reply=true` 且尚未用工具回复时，宿主注入 **ephemeral** system 尾注（不持久化、不进摘要），要求第一个 tool 必须是 `session_send` / `session_multisend` / `session_broadcast`。

---

## 五、HTTP 与 SSE

| 用途 | 方法 / 路径 |
|------|-------------|
| 全局事件流 | `GET /api/events/stream` |
| 发送消息 | `POST /api/chat/send` |
| 人类确认 | `POST /api/chat/user-confirm` |
| 停止生成 | `POST /api/chat/stop` |
| 历史 | `GET /api/chat/history` |
| 健康检查 | `GET /health` |

所有后台 Agent 事件经 **同一 SSE** 推送，带 `conversation_id`；前端按 cid 分发到对应 tab（经典 / 沉浸共用逻辑）。

展示约定：

- 用户消息：左侧用户气泡
- Agent peer 消息：左侧 Agent 气泡（name / cid）
- 本 Agent 回复：assistant 气泡 + 步骤面板

---

## 六、`user_confirm` 边界

`user_confirm` **仅**用于向人类 Boss 确认。

若当前任务来自其他 Agent（最近 user 的 `_sender` 为 peer cid）：

- 不弹人类 UI
- 后端将 `user_confirm` 转为成功 tool 结果，提示用 `session_send` 向发送方澄清
- Agent 间协商、投票、追问一律走 `session_*`

---

## 七、工作模式与写盘

| 模式 | 行为 |
|------|------|
| Auto | 按任务分类自动选择是否写盘 |
| Plan | 拒绝真实写盘；写类 tool 仅 dry_run 预览 |
| Execute | 真实写盘；须先有 `todo_list`；写前 dry_run 预览 |

仅审查意图（`audit_intent_keys`）→ 宿主强制 Plan。

---

## 八、狼人杀验收流程（法官 Agent）

1. `session_create(count=10, name_prefix="玩家", …)`
2. 私发身份：`session_send` × N
3. 开局公告：`session_broadcast(tag=…)`
4. 夜晚：狼人 `session_multisend` + `session_wait(suspend=true)`；神职单独 `session_send` + wait
5. 白天：顺序 `session_send` + wait；投票 `session_multisend` + wait
6. 循环至胜负

覆盖：批量创建、点对点、组播、广播、顺序控制、忙时队列、挂起恢复、前端多 tab 可视化。

---

## 九、并行开发验收（协调 Agent）

1. `session_create` 创建后端 / 前端 / 测试 / 审查 Agent  
2. `session_multisend` 并行派工  
3. 子 Agent `session_send` 回报或互相澄清  
4. 协调 Agent `session_wait(suspend=true)` 汇总  
5. `session_broadcast` 发布集成决策  

---

## 十、相关代码索引

|  Concern | 路径 |
|----------|------|
| 宿主门面 | `agent_v4/agent_core.py` |
| 路由 | `agent_v4/routes.py` |
| 协作状态 | `agent_v4/live_state.py` |
| 工具实现 | `tools/session_*.py` |
| 工具执行 / gate | `agent_v4/core/tool_runtime.py`（含 `file_search` 允许列表、剥离遗留 `action`） |
| 提示词 | `util/agent_prompt_constants.py` |
| 经典前端 | `res/js/agent-ui.js` |
| 沉浸前端 | `res/js/immersive/sse.js` |

> 回归与开发规范见 [`回归测试方案.md`](回归测试方案.md)。
