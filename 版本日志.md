# 版本日志

## 历史

- **v1.3 多 Agent 自由会话网格（Agent Session Mesh）与通讯规范重构**
  - **自由 Agent 管理** `session_create` 支持名字池自动取名、标签隔离（不同 tag 下可复用同名）、创建时自动写入 `.title` 文件。
  - **点对点通讯** `session_send` 新增 `requires_reply` 必填参数，强制显式声明是否需要回复；`_sender_role=boss` 自动识别非 Agent 发送者。
  - **组播与广播** `session_multisend`/`session_broadcast` 同步支持 `requires_reply` 必填参数，`session_broadcast` 支持 tag 筛选与纯通知模式。
  - **等待机制** `session_wait` 新增哨兵安全检查（`wait_without_request`），防止未发请求就等待导致永久挂起；支持 `suspend` 自动挂起恢复。
  - **通讯规范统一** 所有消息元数据字段统一为下划线风格（`requires_reply`、`thread_id`），向后兼容旧驼峰名。
  - **提示词强化** `TEAM_ROLE_DEFAULT` 新增规则：`requires_reply` 必填、无前缀元数据=Boss直接指令；`AGENT_CODE_HINT_SYSTEM_PROMPT` 新增准则10全面复查。
  - **Todo-List 强化** `TOOL_AGENT_V2_SYSTEM_PROMPT` 中【Todo 与模式】规则加固为【强制】，每轮必须 query、每步必须 check、完成后必须 close。

- **v1.2 集成可灵 Kling AI 视频生成与专业 AI 视频导演技能体系**
  - **可灵 API 集成** 文生视频（支持多镜头/运镜控制/首尾帧/主体参考/图生视频）、异步任务查询与自动归档、JWT 认证。
  - **AI视频导演技能库** 引入七节点管线（需求解析→风格锚定→导演稿→关键帧抽卡→分镜验证→细节迭代→终版归档），MCSLA 公式、Identity vs Motion 分离规则（解决人脸漂移）、负向约束参考表（5类16种伪影）、浮夸词替换表、物理参数标注规范、一次只改一个变量迭代规则。
  - **AI视频生成流水线** 8步流程与 todo_list 驱动，std/pro/4k 模式选择与费用告知，多镜头分镜 512 字符规范，单镜头 2500 字符填充。
  - **Higgsfield / Seedance 最佳实践整合** 37种运镜大全（Dolly/Crane/Orbit/FPV等）、声音设计指南（环境音/拟音/音乐/对白）、3个专业类型模板（电影风格/打斗场景/电商广告）、2秒钩子框架、6-Pass 诊断序列。
  - **參考图策略** 首帧/首尾帧/主体参考/多图组合的选择标准与 prompt 写法。
  - **多镜头连贯性** 角色/环境/光照三锚点 + Identity Block + 状态追踪表。
  - **统一归档体系** 视频/result.json/终版提示词.md 统一存入 `workspace/kling_tasks/{task_id}/`，工具代码自动保存、Skill 指引同步更新。
  - **Skill 文档规范** 禁止无台词开口、禁止反重力悬挂、禁止穿模、禁止光滑面虚假反射等物理约束写入 skill 规则。

- **v1.1 新增沉浸模式与白天/夜间界面主题，扩展多栏布局与跨模式会话状态同步**
  - **沉浸模式** 多栏并行会话、分栏宽度可调；支持 `/` 命令与 `@` 文件引用等交互；独立路由 `/immersive`；与经典模式之间可切换并保持当前会话选中一致。
  - **白天/夜间** 全站明暗主题（经典页与沉浸页共用 `localStorage` 记忆），浅色与深色下面板、消息区、知识库与用户确认等控件配色区分；助手消息在浅色下与背板融合、用户消息保留气泡等版式优化。
  - **其他** 浏览器样式 reset、顶栏自动上收动画、知识库与确认卡片勾选样式统一等体验细节。

- **v1.0 重构上下文分段与用量体系，扩展分段占用与底栏可视化，优化用量估算与摘要输入，移除旧版「按最近若干轮单段裁剪」主路径，统一启动与致命报错中的配置说明口径**
  - **上下文** 由「整段按轮数裁剪」改为「两段式」：一段只保留面向用户的连续对话（可折叠），另一段保留与工具相关的完整脉络；下线旧的「只按最近若干轮单段截取」主路径。
  - **摘要** 进模型前去掉工具调用噪声，摘要任务说明更清晰。
  - **用量估算** 按中英文字符习惯做近似估算，便于和账单观感一致。
  - **分段占用展示** 每轮结束后把各段占用交给界面；侧栏用量与分段比例随之更新。
  - **底部用量条** 用比例条展示「对话折叠段 / 工具相关段 / 摘要」各自占比，鼠标悬停可看百分比。
  - **用量区版式** 用量条与底部用量栏的高度、间距和配色调整，信息更集中可读。
  - **启动与报错提示** 启动脚本、托盘与致命报错里的配置路径说明统一为读者认识的配置文件名。

- **v0.9 新增按会话绑定模型与用量累计，扩展流式账单中缓存命中拆分，优化侧栏模型展示与费用预估**
  - **模型与会话** 每个会话单独记住所用模型；模型切换后侧栏展示与预估随之更新。
  - **定价与累计** 接入定价用于费用估算；持久化累计用量。
  - **流式账单拆分** 流式结果里把常规用量与缓存命中用量分开统计。
  - **侧栏模型与费用** 展示当前模型、费用预估与环形用量。
  - **侧栏费用区版式** 环形用量与费用说明区域的排版优化。

- **v0.8 新增三种工作模式持久化、推理档位保存、知识库目录注入与侧栏知识库，扩展斜杠快捷命令面板，优化侧栏相关版式**
  - **工作模式** 自动、规划、执行三种模式可持久化；模式入口与下拉菜单。
  - **推理档位** 档位可保存；侧栏推理档位菜单。
  - **知识库** 指定目录后写入系统提示；侧栏知识库页签与浏览能力。
  - **斜杠快捷命令** 快捷命令面板与选项状态、保存结果一致。
  - **侧栏版式** 模式区、快捷命令层、推理菜单与知识库区域视觉统一。

- **v0.7 新增浏览器多标签会话与 Windows 托盘单实例入口，扩展加密落盘与停止回滚能力，优化多标签主界面版式**
  - **会话数据** 多会话列表与加密落盘；停止与回滚类操作。
  - **标签页** 浏览器内多标签页；会话切换、关闭标签。
  - **Windows 托盘** 单实例托盘入口；从托盘唤起。
  - **其他系统** 非 Windows 环境直接启动服务。
  - **主界面版式** 多标签与主内容区布局。

- **v0.6 新增长对话超阈值异步摘要、落盘合并与工具回合扩范围，新增摘要并发短时锁**
  - **摘要流水线** 消息超过阈值后异步生成摘要并落盘，再合并回会话。
  - **摘要范围** 覆盖范围扩展到相关工具回合。
  - **并发控制** 并发生成摘要用短时锁避免冲突。

- **v0.5 优化对话前缀稳定性以适配前缀缓存计费，扩展账单流中缓存命中区分，新增侧栏环形用量与费用预估展示**
  - **前缀与缓存计费** 为「前缀缓存」类计费优化对话前缀稳定性；账单流里区分普通 token 与缓存命中。
  - **侧栏费用与用量** 费用预估与环形用量。
  - **用量环版式** 用量环与费用说明区域排版。

- **v0.4 新增兼容常见对话补全协议、服务端推送事件流、子进程脚本调度工具，扩展聊天主区与步骤面板，优化流式阅读与步骤展开**
  - **对话协议** 兼容常见对话补全接口形态。
  - **事件流** 服务端推送式事件流。
  - **工具执行** 工具在受控子进程或脚本中调度执行。
  - **聊天与步骤** 聊天主区与步骤过程面板。
  - **消息与步骤样式** 消息气泡与步骤区基础样式。
  - **流式阅读** 流式逐字展示与步骤展开查看。

- **v0.3 规范化工具集中维护与模型侧描述生成，扩展失败时用法说明注入与畸形参数容错**
  - **工具描述与注册** 集中维护工具说明并生成模型侧工具描述。
  - **参数与容错** 参数映射；失败时附带用法说明；对异常参数做容错。

- **v0.2 扩展本地文件与目录类工具：读、写、区间替换、路径列举、复制移动重命名安全删除、正则或字面定位、目录树关键字检索**
  - **文件与目录** 文件读取、整文件写入、文件内区间替换；按 glob 规则列举工作区路径；文件复制、移动、重命名与安全删除；用正则或固定字面量在文件中定位；在目录树内按关键字全文检索。

- **v0.1 首次发布 Web 服务与工具调用骨架，内置四项演示工具，提供最小网页对话壳与基础单页版式**
  - **服务骨架** Web 服务与工具调用框架。
  - **演示工具范围** 仅四项：公网 IP 解析地理位置、按地区查询天气预报、抓取网页并抽取正文文本、探测本机常用命令是否可用。
  - **对话网页壳** 最小可用网页对话界面。
  - **首屏版式** 基础单页布局。
  - **首轮形态** 首轮对话与工具调用雏形。



## v1.3 (2026-05-19)
- 多 Agent 团队协作：TL 监督、Worker 执行、QA 验收
- 团队通讯工具：session_send/broadcast/team/report/create
- SSE 多播观察者机制：TL 实时查看 Worker 执行日志
- 团队仪表盘 /team
- Skills 系统升级：frontmatter 标准化、动态扫描、旁支模型生成元数据
- 代号池配置
