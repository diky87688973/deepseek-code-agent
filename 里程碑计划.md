# 里程碑计划（Roadmap）

> 从 0.1 到 1.0 的演进历程，以及 2.0 的未来规划

---

## 已完成的里程碑

### v0.1 — 基础能力期

**目标：** 具备基本的网络查询能力，验证 AI + Tool Calling 架构可行性


| 功能      | 说明                                     |
| ------- | -------------------------------------- |
| IP 地理定位 | cli_ip_geolocate — 通过 IP 解析地理位置，demo工具 |
| 天气查询    | cli_open_meteo_weather — 查询天气数据，demo工具 |
| Web 抓取  | cli_web_fetch — 抓取网页文本内容               |
| 环境探测    | cli_env_probe — 探测本机命令环境               |


**架构：** 最简 FastAPI + OpenAI-compatible 客户端，硬编码 system prompt

---

### v0.2 — 本地能力扩展期

**目标：** 让 AI 能够操作本地文件，从「查询工具」进化为「操作工具」


| 功能   | 说明                                  |
| ---- | ----------------------------------- |
| 文本编辑 | read_file / write_file / replace_in_file — 读写与区间替换 |
| 目录浏览 | glob_files — 按 glob 列举工作区路径         |
| 文件操作 | file_ops / delete_file — 复制、移动、重命名、安全删除 |
| 正则定位 | regex_locate / find_in_file — 正则或字面定位 |
| 内容搜索 | grep_files — 目录内全文检索          |


**架构：** 引入进程内工具执行机制，agent_main() 直接调用代替子进程

---

### v0.3 — 统一规范期

**目标：** 工具库从「临时扩充」走向「标准化管理」


| 功能   | 说明                                               |
| ---- | ------------------------------------------------ |
| 统一清单 | tool_list_cli.json — 所有工具的 Schema 集中管理           |
| 自动注册 | catalog_to_openai_tools() — 动态生成 OpenAI tools 定义 |
| 参数映射 | CLI 参数到 Python 函数参数自动映射                          |
| 帮助捕获 | 工具调用失败时自动注入 --help                               |
| 参数修复 | 容错处理模型生成的畸形参数                                    |


**规范：** 工具为 `tools/<name>.py`（无 `cli_` 前缀），统一返回 `{ok, data, error}` 三字段 JSON

---

### v0.4 — 多会话支持期

**目标：** 从单次对话进化为持久化多会话管理


| 功能      | 说明                     |
| ------- | ---------------------- |
| 多标签页    | Web UI 支持同时管理最多 8 个会话  |
| 对话持久化   | 加密存储到磁盘，重启后自动恢复        |
| 自动标题    | LLM 自动生成会话标题           |
| 会话列表    | 按日期分组的会话管理             |
| UI 状态保存 | localStorage + 服务端双持久化 |


**加密：** 引入 Windows DPAPI 和本地密钥双轨加密方案

---

### v0.5 — KV 缓存利好设计期

**目标：** 充分利用 DeepSeek 等模型的 prefix caching 机制，降低使用成本


| 功能   | 说明                                      |
| ---- | --------------------------------------- |
| 用量上报 | 流式响应中携带 token 用量（cache_hit_tokens）      |
| 计费模型 | 区分 cache_hit / cache_miss / output 三档价格 |
| 用量累计 | 持久化累计 token 消耗                          |
| 定价查询 | REST API 实时查询模型费用                       |
| 自动降级 | 模型拒绝 stream_options 时自动移除并重试            |


**设计理念：** 保持系统提示词和工具定义的稳定性，最大化前缀缓存命中率

---

### v0.6 → v1.0 — 长期记忆期（当前版本）

**目标：** 解决长对话上下文窗口限制，让 AI 能记住更早的对话

#### v0.6 状态（当前实现 — 长期记忆 0.1）


| 功能     | 说明                               |
| ------ | -------------------------------- |
| 上下文裁剪  | 800 条消息触发压缩阈值                    |
| LLM 摘要 | 调用模型自动压缩历史对话                     |
| 摘要持久化  | 写入 excerpts 目录，YAML header 携带元信息 |
| 工具链完整性 | 摘要切片自动扩展到完整工具调用链                 |
| 防并发锁   | 带 TTL 防止重复压缩                     |
| 保留窗口   | 保留最近 60 轮完整对话                    |


**已知局限：**

- 摘要由通用 LLM 生成，非专用记忆模型，存在信息丢失风险
- 纯文本摘要，无向量化索引，无法精确检索
- 压缩触发后才被动处理，非实时记忆更新

#### v1.0 稳定版（当前）


| 功能        | 说明                          |
| --------- | --------------------------- |
| 完整工具链     | 20+ 工具覆盖文件、代码、Git、网络、诊断     |
| 三种执行模式    | Auto / Plan / Execute 全流程支持 |
| 系统托盘      | Windows 托盘图标管理              |
| 知识库 RAG   | 文件勾选后注入 System Prompt       |
| 单页 Web UI | Dark 主题，无框架依赖               |
| 安全机制      | safeMode、回收站、加密存储           |


---

## 未来规划

### v2.0 — 多模态 + 记忆系统

**核心目标：** 从纯文本交互进化为多模态交互，引入专用记忆模型

#### 主线一：多模态接入


| 功能       | 说明                  | 优先级 |
| -------- | ------------------- | --- |
| 语音入口     | ASR 语音识别转文本输入，支持多语种 | P0  |
| 图片入口     | 图片上传，多模态大模型理解，图文对话  | P0  |
| 被调多模态大模型 | 接入支持图文/音视频的多模态 LLM  | P0  |


#### 主线二：记忆模型 1.0 接入

**现状：** 通用 LLM 做文本摘要，被动触发，无专用记忆架构

**目标：** 接入专用记忆模型，实现类人记忆机制


| 功能       | 说明                                       |
| -------- | ---------------------------------------- |
| 自然语言记忆   | 记忆模型以自然语言形式存储和回忆对话中的关键信息                 |
| 海马体回放训练  | 夜间静息时间（低负载时段），记忆模型自我回放巩固，类似生物海马体的睡眠期记忆固化 |
| 本地小模型    | 在本地运行轻量级记忆模型，支持夜间定时自我训练                  |
| 实时记忆写入   | 对话过程中实时提取关键信息写入记忆存储，无需等待压缩触发             |
| RAG 增强检索 | 在现有知识库基础上，引入向量数据库 + 语义检索 + 重排序           |


#### 研究探索方向


| 方向       | 说明                                           |
| -------- | -------------------------------------------- |
| 语义向量空间投影 | 将记忆映射到高维语义空间，支持非自然语言的记忆检索（如「三个月前那个报错」→ 向量匹配） |
| 非自然语言检索  | 支持通过情绪、时间范围、模糊语义等非精确表达检索记忆                   |


---

### v2.0 旁支版本计划

#### 旁支一：嵌入式工具库


| 目标          | 说明                                     |
| ----------- | -------------------------------------- |
| Web 端可嵌入    | 将 Web UI 改造为可嵌入第三方系统的组件式工具库            |
| C/C++ 重写工具库 | 将核心 `tools/*.py` 工具用 C/C++ 重写，提升性能并支持更广泛的嵌入场景 |
| 提供 SDK      | 对外提供 SDK / API 供其他应用集成调用               |


#### 旁支二：决策控制系统 1.0


| 目标    | 说明                              |
| ----- | ------------------------------- |
| 高频调度  | 引入独立的决策控制系统，支持大模型进行毫秒级高频工具调度和反馈 |
| 闭环反馈  | 工具执行结果实时反馈给决策模块，动态调整下一步策略       |
| 可配置策略 | 支持自定义调度策略（优先级、超时、重试、降级）         |


#### 旁支三：运动神经网络


| 目标     | 说明                            |
| ------ | ----------------------------- |
| 神经网络控制 | 探索基于神经网络的运动控制能力，从纯数字世界向物理世界延伸 |
| 硬件接口   | 支持通过工具调用控制外部硬件设备              |


---

## 版本时间线

```
v0.1 ─── v0.2 ─── v0.3 ─── v0.4 ─── v0.5 ─── v0.6 ─── v1.0
  │        │        │        │        │        │        │
  │        │        │        │        │        │        └─ 当前稳定版
  │        │        │        │        │        │
  │        │        │        │        │        └─ 长期记忆 0.1（当前）
  │        │        │        │        │
  │        │        │        │        └─ KV 缓存利好设计
  │        │        │        │
  │        │        │        └─ 多会话支持
  │        │        │
  │        │        └─ 统一规范
  │        │
  │        └─ 本地文件编辑
  │
  └─ IP/天气查询

──────────────────────────────────────────────► 时间

                    ┌─────────────────────────────────┐
                    │     v2.0 多模态 + 记忆系统        │
                    │  语音 │ 图片 │ 记忆模型 │ 决策控制  │
                    │  RAG 增强 │ 嵌入式工具库 │ 运动网络 │
                    └─────────────────────────────────┘
```

---

## 版本号说明


| 版本   | 说明                          |
| ---- | --------------------------- |
| v0.x | 能力积累期，每期增加一个核心能力维度          |
| v1.0 | 首个稳定版，工具链完整、架构成熟、可用性达标      |
| v2.0 | 多模态 + 记忆系统，从「工具助手」进化为「智能伙伴」 |


---

> 最后更新：2026 年

