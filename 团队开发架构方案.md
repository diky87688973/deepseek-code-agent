# Agent Session Mesh 多 Agent 协作方案

> **版本**：v2.0  
> **updated**：2026-05-22  
> **状态**：已落地到 `agent_v2`，经典页是主入口

---

## 一、目标

本方案用于支撑真实多 Agent 并行协作：开发、审查、测试、协调和复杂多人流程。狼人杀只是验收场景：如果系统能稳定组织 10 个 Agent 完成一局狼人杀，说明点对点、小组私聊、广播、顺序控制、并发运行和可视化能力基本达标。

---

## 二、核心模型

一个会话就是一个 Agent：

```text
Agent = conversation_id + name + role + persona + tags + inbox + state
```

原则：

- Agent 消息持久化仍使用 `role="user"`，不引入 `role=CID`，避免破坏 OpenAI/DeepSeek 兼容协议、上下文摘要和远期折叠逻辑。
- Agent 来源通过私有字段和正文前缀表达。

消息示例：

```json
{
  "role": "user",
  "content": "[from=玩家1 | sender_cid=uuid-a | threadId=day1-speak-1 | channel=direct | requiresReply=true]\n请发言",
  "_sender": "uuid-a",
  "_sender_name": "玩家1",
  "_sender_role": "狼人杀玩家",
  "_agent_peer_message": true,
  "_message_kind": "peer",
  "_channel": "direct",
  "_thread_id": "day1-speak-1",
  "_requires_reply": true
}
```

这些消息与用户消息一样参与摘要压缩、远期记忆折叠、近期窗口和工具调用链处理。

---

## 三、持久化状态

### 3.1 Agent Registry

运行时状态在 `agent_v2/live_state.py`：

```python
AGENT_SESSIONS: Dict[str, Dict[str, Any]] = {}
```

持久化文件：

```text
{DATA_ROOT}/agent_sessions.json
```

Agent 元数据：

```json
{
  "cid": "uuid",
  "name": "玩家1",
  "role": "狼人杀玩家",
  "persona": "你是狼人杀玩家1...",
  "tags": ["alive", "werewolf"],
  "status": "idle",
  "created_by": "boss-cid",
  "created_at": 1770000000,
  "updated_at": 1770000000
}
```

### 3.2 持久化 Inbox

目标 Agent 忙碌时，消息进入持久化 inbox：

```text
{DATA_ROOT}/agent_inbox/{cid}.jsonl
```

当前 run 结束后，系统自动取下一条消息写入目标会话并启动下一轮运行。目标忙时不会丢消息，也不要求发送方重复发送。

---

## 四、通讯工具

### 4.1 `session_create.py`

创建一个或多个自由 Agent。

```json
{
  "action": "create",
  "count": 10,
  "name_prefix": "玩家",
  "role": "狼人杀玩家",
  "persona": "你是狼人杀玩家，请按收到的信息和规则行动。",
  "tags": ["alive"]
}
```

返回：

```json
{
  "agents": [
    {"cid": "uuid-1", "name": "玩家1"},
    {"cid": "uuid-2", "name": "玩家2"}
  ],
  "count": 10
}
```

`persona/tags/name/role` 会动态注入该 Agent 的上下文前缀，保证每轮请求模型时都能看到身份设定。

### 4.2 `session_send.py`

点对点发送。

```json
{
  "action": "send",
  "target_id": "uuid-b",
  "threadId": "day1-speak-1",
  "channel": "direct",
  "requiresReply": true,
  "message": "请按顺序发言。"
}
```

行为：

- 目标空闲：立即写入目标会话并后台启动目标 Agent。
- 目标忙碌：进入目标持久化 inbox。
- 消息正文包含 `from/sender_cid/threadId/channel/requiresReply` 元数据，确保模型能读到。

### 4.3 `session_multisend.py`

向指定小组发送同一条消息，用于小组私聊、并行派工、狼人夜间沟通等。

```json
{
  "action": "send",
  "target_ids": ["uuid-wolf-1", "uuid-wolf-2", "uuid-wolf-3"],
  "threadId": "night-1-wolves",
  "channel": "werewolves-night-1",
  "message": "狼人请讨论今晚击杀目标。"
}
```

### 4.4 `session_broadcast.py`

广播给所有 Agent，或按 `role/tag` 筛选。

```json
{
  "action": "broadcast",
  "tag": "alive",
  "message": "天亮了，请所有存活玩家准备发言。"
}
```

### 4.5 `session_list.py`

列出自由 Agent。

```json
{
  "action": "list"
}
```

返回 `cid/name/role/status/tags/queueSize`。

### 4.6 `session_wait.py`

检查指定 Agent 是否已回复某个 `threadId`。用于发言顺序和任务汇总。

```json
{
  "action": "wait",
  "target_ids": ["uuid-player-1"],
  "threadId": "day1-speak-1",
  "suspend": true
}
```

返回：

```json
{
  "completed": ["uuid-player-1"],
  "pending": [],
  "messages": [{"from": "uuid-player-1", "content": "..."}],
  "all_done": true
}
```

推荐使用 `action="wait"` 或 `suspend=true`。如果目标尚未回复，后端会挂起当前 Agent run，当前轮立即结束；当目标 Agent 之后通过 `session_send` 向当前会话发送匹配 `_sender/threadId` 的消息时，系统会自动恢复等待方继续运行。

`action="check"` 仍可用于普通状态检查，但不应在同一轮反复轮询。

---

## 五、运行与前端事件

经典页是主入口。页面启动后建立一个全局 SSE：

```text
GET /api/events/stream
```

发送用户消息：

```text
POST /api/chat/send
```

提交人类确认：

```text
POST /api/chat/user-confirm
```

`agent_v2` 已删除旧接口：

```text
POST /api/chat/stream
POST /api/chat/user-confirm/stream
```

所有后台 Agent 事件都通过全局 SSE 推送，事件携带 `conversation_id`，前端按 cid 分发到对应 tab。

前端展示：

- 用户消息：普通左侧用户气泡。
- Agent 消息：左侧独立 Agent 气泡，显示 `name / cid`。
- Agent 回复：对应会话 tab 中的 assistant 气泡。
- 后台运行过程：对应 tab 的步骤、流式输出、错误、done 都通过全局 SSE 更新。

---

## 六、`user_confirm` 规则

`user_confirm` 只用于请求人类用户确认。

当当前任务来源是其他 Agent（最近 user 消息 `_sender` 是其他会话 ID）时：

- 不弹人类确认。
- 后端把 `user_confirm` 转成成功工具结果，提示该 Agent 使用 `session_send` 向发送方澄清。
- Agent 之间的疑问、确认、投票、协商都通过 `session_send/session_multisend/session_broadcast` 完成。

---

## 七、狼人杀验收流程

法官 Agent 应能完成：

1. `session_create(count=10, name_prefix="玩家", role="狼人杀玩家", tags=["alive"])`
2. 给每个玩家私发身份：`session_send`
3. 全员公告游戏开始：`session_broadcast`
4. 夜晚：
   - 狼人小组私聊：`session_multisend(target_ids=狼人列表, channel="werewolves-night-1")`
   - 等待狼人结果：`session_wait(action="wait", threadId="night-1-wolves")`
   - 预言家/女巫等单独行动：`session_send + session_wait(action="wait")`
5. 白天：
   - 广播死亡信息
   - 按顺序 `session_send(玩家N, threadId="day1-speak-N")`
   - 每个玩家发言后 `session_wait(action="wait")`
   - 投票阶段 `session_multisend` 给存活玩家
   - `session_wait(action="wait")` 收集投票
6. 循环直到胜负。

该验收覆盖：批量创建、点对点、小组私聊、全员广播、顺序控制、并发执行、忙时队列、前端可视化。

---

## 八、并行开发验收流程

协调 Agent 可：

1. `session_create` 创建后端、前端、测试、审查 Agent。
2. `session_multisend` 并行派发任务。
3. 各 Agent 通过 `session_send` 回报结果或互相澄清。
4. 协调 Agent 用 `session_wait(action="wait")` 等待各线程结果，目标回复后自动恢复。
5. `session_broadcast` 发布集成决策或下一轮任务。

---

## 九、遗留兼容说明

- `deepseek_code_agent.py` 是 v1 单体，仍可能包含旧 stream 路由；当前主实现是 `deepseek_code_agent2.py + agent_v2`。
- 新能力以 `agent_v2`、经典页 `/`、全局 SSE 和 Agent Session Mesh 工具为准。
