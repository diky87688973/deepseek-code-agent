# 进行中的拓展工具

> **定位**：通用 Coding Agent 能力地图 — 已实现清单 + 待拓展缺口  
> **catalog 来源**：`tools/tool_list_agent.json`（38 个 function）  
> **最后更新**：2026-05-21

---

## 一、通用 Agent 能力域

| 能力域 | 通用 Agent 应覆盖 | 当前状态 |
|--------|-------------------|----------|
| 感知与检索 | 读文件、搜索、glob、诊断、网页抓取 | ✅ 已覆盖 |
| 编辑与变更 | 写/替换/patch/删除、diff、表格 | ✅ 已覆盖 |
| 执行与验证 | 命令、内联 Python、Git 读、诊断 | ⚠️ Git 仅读；无专用格式化 |
| 规划与协作 | todo、模式、用户确认、多 Agent Mesh | ✅ 已覆盖 |
| 知识与技能 | Skill 管理、知识库注入 | ✅ Skill；向量记忆待建 |
| 外部集成 | HTTP、GitHub API、天气/IP | ✅ 基础覆盖 |
| 多模态 | OCR、AI 生图/视频 | ✅ OCR + Kling/Dreamina |
| 数据与文档 | CSV/Excel、压缩包 | ⚠️ 无 PDF / DB 查询 |
| 系统运维 | 环境探测 | ⚠️ 无 psutil 级监控 |

---

## 二、已实现工具（按能力域）

> function 名与 catalog 一致；完整 schema 见 `tool_list_agent.json`。

### 感知与检索

| function | 说明 |
|----------|------|
| `read_file` | 全文 / 行区间 / 字符区间 |
| `grep_files` | 目录内容搜索 |
| `find_in_file` | 单文件定位 |
| `regex_locate` | 正则定位 |
| `file_search` | 全文搜索 |
| `glob_files` | 路径列举 |
| `web_fetch` | HTTP 抓取文本 |
| `web_fetch_render` | 抓取并渲染可读正文 |
| `unified_diagnose` | 语法 / ruff 诊断 |
| `env_probe` | 运行环境快照 |

### 编辑与变更

| function | 说明 |
|----------|------|
| `write_file` | 写文件（dry_run 默认） |
| `read_write` | 管道式读写 |
| `replace_in_file` | 字面 / rules / 区间替换 |
| `apply_patch` | unified diff 补丁 |
| `file_ops` | copy / move / rename |
| `delete_file` | 安全删除 |
| `text_diff` | 文本 / 文件 diff |
| `data_table` | CSV/Excel 预览与筛选 |
| `archive` | zip 压缩 / 解压 / 列表 |

### 执行与验证

| function | 说明 |
|----------|------|
| `run_command` | 子进程命令（黑名单 + safeMode） |
| `python_inline` | 内联 Python |
| `git_workspace` | status / log / diff（只读） |
| `run_type` | Auto / Plan / Execute |

### 规划与协作

| function | 说明 |
|----------|------|
| `todo_list` | 执行清单（Execute 写盘前置） |
| `user_confirm` | 人类 Boss 确认（peer 场景自动转 session） |
| `session_create` | 批量创建 Agent |
| `session_send` | 点对点（`requires_reply` 必填） |
| `session_multisend` | 小组同播 |
| `session_broadcast` | 按 tag 广播 |
| `session_list` | Mesh 列表 |
| `session_wait` | check / wait + 挂起恢复 |

### 知识与技能

| function | 说明 |
|----------|------|
| `skill_manage` | 列出 / 加载 Skill |

### 外部集成

| function | 说明 |
|----------|------|
| `github_api` | GitHub REST（issue / PR / repo 等） |
| `ip_geolocate` | IP 地理 |
| `open_meteo_weather` | 天气 |

### 多模态

| function | 说明 |
|----------|------|
| `image_ocr` | 图片 OCR |
| `kling_generate` | 可灵视频生成 |
| `dreamina_generate` | Dreamina 图片/视频 |

---

## 三、待拓展（按优先级）

### P0 — 编码 Agent 高频缺口

| 候选 function | 价值 | 依赖 | 难度 |
|---------------|------|------|------|
| `format_code` | ruff/black/prettier 格式化与 check | ruff / black | ⭐ |
| `git_write` | add / commit / push / branch | Git CLI | ⭐⭐ |
| `structured_editor` | JSON/YAML/INI 按路径读写 | 标准库 | ⭐⭐ |
| `rename_symbol` | 跨文件符号重命名 | grep + replace | ⭐⭐⭐ |

### P1 — 数据与集成

| 候选 function | 价值 | 依赖 | 难度 |
|---------------|------|------|------|
| `pdf` | PDF 文本 / 表格 / 合并拆分 | PyMuPDF | ⭐⭐ |
| `db_query` | SQLite / MySQL / PG 只读查询 | sqlite3 + 驱动 | ⭐⭐⭐ |
| `api_test` | REST 调试（method/header/body） | httpx | ⭐⭐ |
| `pkg_manager` | pip / npm install 与 list | 系统 CLI | ⭐⭐ |

### P2 — 运维与效率

| 候选 function | 价值 | 依赖 | 难度 |
|---------------|------|------|------|
| `sys_info` | CPU / 内存 / 磁盘 / 进程 Top | psutil | ⭐ |
| `codec` | Base64 / hash / URL 编解码 | 标准库 | ⭐ |
| `network_diag` | ping / DNS / 端口 | ping3 | ⭐⭐ |
| `convert_file` | CSV↔JSON、MD→HTML、编码转换 | pandas 可选 | ⭐⭐ |
| `file_dedupe` | 重复文件查找 | 标准库 | ⭐⭐ |

### P3 — v2.0 架构级

| 方向 | 说明 |
|------|------|
| 向量记忆 | `embedding_search`、专用记忆写入/回放 |
| 多模态入口 | ASR、图片上传、多模态 LLM |
| 决策控制 | 高频调度闭环（旁支） |

### P3 — 锦上添花

`batch_rename`、`markdown_convert`、`clipboard`、`screenshot`、`diff_dir`、`qrcode`、`translate`、`timer_cron`、`process_manage`、`env_edit`、`websocket_client`、`img_tool`（本地 PIL 处理）、`email` 等 — 按业务需求排期。

---

## 四、P0 工具参数草案（实现时参照）

### `format_code`

```json
{ "path": "src/app.py", "formatter": "ruff", "check": false, "dry_run": true }
{ "path": "src/", "glob_pattern": "*.py", "formatter": "ruff", "check": true }
```

### `git_write`

```json
{ "action": "commit", "message": "fix: 登录bug", "add_all": true, "dry_run": true }
{ "action": "push", "remote": "origin", "branch": "main" }
{ "action": "checkout", "branch": "feature/new", "create": true }
```

### `structured_editor`

```json
{ "action": "read", "path": "config.json", "key": "server.port" }
{ "action": "set", "path": "config.json", "key": "server.port", "value": 8080, "dry_run": true }
```

---

## 五、新增工具流程（必遵）

完整规范见 [`回归测试方案.md`](回归测试方案.md) §开发规范。摘要：

1. 实现 `tools/<name>.py`，入口 `agent_main(**kwargs) -> dict`
2. 注册 `tool_list_agent.json`：`name` 对应文件名；`function` 与 purpose 含 **完整 function 名**；多 action 写 `action=xxx`
3. 参数 **snake_case**；写操作默认 `dry_run=true`
4. 跑 Layer 0：`python -m unittest discover -s tests -v` + `check_symbols.py`
5. 在回归文档补手工用例后再合入

---

## 六、相关文档

| 文档 | 用途 |
|------|------|
| [`回归测试方案.md`](回归测试方案.md) | 自动化 + 手工回归 + 开发规范 |
| [`团队开发架构方案.md`](团队开发架构方案.md) | Session Mesh 协作 |
| [`里程碑计划.md`](里程碑计划.md) | 版本里程碑 |
