# 里程碑计划（Roadmap）

> 从 0.1 到 1.0 的演进与 2.0 规划。**详细变更以 [`版本日志.md`](版本日志.md) 为事实依据**；本页按里程碑归纳，与之一致。

---

## 已完成的里程碑（与版本日志对齐）

### v0.1 — 首版骨架

**版本日志：** 首次发布 Web 服务与工具调用骨架，内置四项演示工具，提供最小网页对话壳与基础单页版式。

| 方向     | 说明 |
| -------- | ---- |
| 服务骨架 | Web 服务与工具调用框架 |
| 演示工具 | 公网 IP 地理位置、天气预报、网页正文抓取、本机命令探测 |
| 对话 UI  | 最小可用网页对话界面与首屏版式、首轮对话与工具雏形 |

---

### v0.2 — 本地文件与目录工具

**版本日志：** 扩展本地文件与目录类工具（读、写、区间替换、路径列举、复制移动重命名安全删除、正则或字面定位、目录树关键字检索）。

| 方向   | 说明 |
| ------ | ---- |
| 读与写 | 文件读取、整文件写入、文件内区间替换 |
| 路径   | 按 glob 列举工作区路径 |
| 文件管理 | 复制、移动、重命名与安全删除 |
| 定位与检索 | 正则或字面量定位；目录树内关键字全文检索 |

---

### v0.3 — 工具规范化

**版本日志：** 规范化工具集中维护与模型侧描述生成；扩展失败时用法说明注入与畸形参数容错。

| 方向   | 说明 |
| ------ | ---- |
| 描述与注册 | 集中维护工具说明并生成模型侧工具描述 |
| 参数与容错 | 参数映射；失败时附带用法说明；异常参数容错 |

---

### v0.4 — 协议、事件流与聊天 / 步骤 UI

**版本日志：** 新增兼容常见对话补全协议、服务端推送事件流、子进程脚本调度工具；扩展聊天主区与步骤面板；优化流式阅读与步骤展开。

| 方向   | 说明 |
| ------ | ---- |
| 对话协议 | 兼容常见对话补全接口形态 |
| 事件流 | 服务端推送式事件流 |
| 工具执行 | 工具在受控子进程或脚本中调度执行 |
| 聊天与步骤 | 聊天主区与步骤过程面板；气泡与步骤区基础样式；流式展示与步骤展开 |

---

### v0.5 — 前缀缓存与侧栏用量

**版本日志：** 优化对话前缀稳定性以适配前缀缓存计费；扩展账单流中缓存命中区分；新增侧栏环形用量与费用预估展示。

| 方向   | 说明 |
| ------ | ---- |
| 前缀与计费 | 为前缀缓存类计费优化对话前缀稳定性；账单流区分常规 token 与缓存命中 |
| 侧栏 | 费用预估与环形用量；用量环与费用说明区域排版 |

---

### v0.6 — 长对话摘要流水线

**版本日志：** 新增长对话超阈值异步摘要、落盘合并与工具回合扩范围；新增摘要并发短时锁。

| 方向   | 说明 |
| ------ | ---- |
| 摘要流水线 | 消息超过阈值后异步生成摘要并落盘，再合并回会话 |
| 摘要范围 | 覆盖范围扩展到相关工具回合 |
| 并发控制 | 并发生成摘要用短时锁避免冲突 |

---

### v0.7 — 多标签、托盘与加密落盘

**版本日志：** 新增浏览器多标签会话与 Windows 托盘单实例入口；扩展加密落盘与停止回滚能力；优化多标签主界面版式。

| 方向   | 说明 |
| ------ | ---- |
| 会话数据 | 多会话列表与加密落盘；停止与回滚类操作 |
| 标签页 | 浏览器内多标签页；会话切换、关闭标签 |
| Windows 托盘 | 单实例托盘入口；从托盘唤起 |
| 其他系统 | 非 Windows 环境直接启动服务 |
| 主界面 | 多标签与主内容区布局 |

---

### v0.8 — 工作模式、推理档位与知识库

**版本日志：** 新增三种工作模式持久化、推理档位保存、知识库目录注入与侧栏知识库；扩展斜杠快捷命令面板；优化侧栏相关版式。

| 方向   | 说明 |
| ------ | ---- |
| 工作模式 | 自动、规划、执行三种模式可持久化；模式入口与下拉菜单 |
| 推理档位 | 档位可保存；侧栏推理档位菜单 |
| 知识库 | 指定目录后写入系统提示；侧栏知识库页签与浏览 |
| 斜杠命令 | 快捷命令面板与选项状态、保存结果一致 |
| 侧栏版式 | 模式区、快捷命令层、推理菜单与知识库区域视觉统一 |

---

### v0.9 — 按会话模型与用量累计

**版本日志：** 新增按会话绑定模型与用量累计；扩展流式账单中缓存命中拆分；优化侧栏模型展示与费用预估。

| 方向   | 说明 |
| ------ | ---- |
| 模型与会话 | 每个会话单独记住所用模型；切换后侧栏展示与预估随之更新 |
| 定价与累计 | 接入定价用于费用估算；持久化累计用量 |
| 流式账单 | 流式结果里把常规用量与缓存命中用量分开统计 |
| 侧栏 | 当前模型、费用预估与环形用量；费用区版式优化 |

---

### v1.0 — 重构上下文分段与用量体系

**版本日志（原文要点）：** 重构上下文分段与用量体系，扩展分段占用与底栏可视化，优化用量估算与摘要输入，移除旧版「按最近若干轮单段裁剪」主路径，统一启动与致命报错中的配置说明口径。

| 方向   | 说明 |
| ------ | ---- |
| 上下文 | 由「整段按轮数裁剪」改为两段式：一段保留面向用户的连续对话（可折叠），另一段保留与工具相关的完整脉络；下线旧的单段截取主路径 |
| 摘要 | 进模型前去掉工具调用噪声；摘要任务说明更清晰 |
| 用量估算 | 按中英文字符习惯做近似估算，与账单观感一致 |
| 分段占用 | 各段占用交给界面；侧栏用量与分段比例随之更新 |
| 底部用量条 | 比例条展示各段占比；悬停可看百分比（后续迭代为「上下文视图」等，仍以版本日志为准） |
| 用量区版式 | 用量条与底部栏高度、间距与配色调整 |
| 启动与报错 | 启动脚本、托盘与致命报错中的配置路径说明统一 |

**实现落点（便于对照代码，非版本日志正文）：**

| 文件 | 要点 |
| ---- | ---- |
| `deepseek_code_agent.py` | `_build_context_segments` / `_build_api_messages_for_model`；`_split_pure_and_full_dialogue`、`_fold_pure_window_for_api`、摘要与 `context_layout` SSE 等 |
| `util/config_loader.py` | `[context]` 与 `AGENT_*` 映射（如 `full_user_rounds`、`pure_user_rounds`、token 估算系数等） |
| `config.ini` | 与上述配置示例同步 |
| `res/js/agent-ui.js` / `res/css/agent-ui.css` | 用量区、`context_layout` 与上下文视图展示 |

---

## 未来规划

### v2.0 — 多模态 + 记忆系统

**核心目标：** 从纯文本交互进化为多模态交互，引入专用记忆模型。

#### 主线一：多模态接入

| 功能       | 说明                  | 优先级 |
| -------- | ------------------- | --- |
| 语音入口     | ASR 语音识别转文本输入，支持多语种 | P0  |
| 图片入口     | 图片上传，多模态大模型理解，图文对话  | P0  |
| 被调多模态大模型 | 接入支持图文/音视频的多模态 LLM  | P0  |

#### 主线二：记忆模型 1.0 接入

**现状：** 通用 LLM 做文本摘要，被动触发，无专用记忆架构。

**目标：** 接入专用记忆模型，实现类人记忆机制。

| 功能       | 说明                                       |
| -------- | ---------------------------------------- |
| 自然语言记忆   | 记忆模型以自然语言形式存储和回忆对话中的关键信息                 |
| 海马体回放训练  | 夜间静息时间（低负载时段），记忆模型自我回放巩固，类似生物海马体的睡眠期记忆固化 |
| 本地小模型    | 在本地运行轻量级记忆模型，支持夜间定时自我训练                  |
| 实时记忆写入   | 对话过程中实时提取关键信息写入记忆存储，无需等待压缩触发             |
| RAG 增强检索 | 在现有知识库基础上，引入向量数据库 + 语义检索 + 重排序           |

#### 研究探索方向

| 方向       | 说明                                           |
| -------- | -------------------------------------------- |
| 语义向量空间投影 | 将记忆映射到高维语义空间，支持非自然语言的记忆检索（如「三个月前那个报错」→ 向量匹配） |
| 非自然语言检索  | 支持通过情绪、时间范围、模糊语义等非精确表达检索记忆                   |

---

### v2.0 旁支版本计划

#### 旁支一：嵌入式工具库

| 目标          | 说明                                     |
| ----------- | -------------------------------------- |
| Web 端可嵌入    | 将 Web UI 改造为可嵌入第三方系统的组件式工具库            |
| C/C++ 重写工具库 | 将核心 `tools/*.py` 工具用 C/C++ 重写，提升性能并支持更广泛的嵌入场景 |
| 提供 SDK      | 对外提供 SDK / API 供其他应用集成调用               |

#### 旁支二：决策控制系统 1.0

| 目标    | 说明                              |
| ----- | ------------------------------- |
| 高频调度  | 引入独立的决策控制系统，支持大模型进行毫秒级高频工具调度和反馈 |
| 闭环反馈  | 工具执行结果实时反馈给决策模块，动态调整下一步策略       |
| 可配置策略 | 支持自定义调度策略（优先级、超时、重试、降级）         |

#### 旁支三：运动神经网络

| 目标     | 说明                            |
| ------ | ----------------------------- |
| 神经网络控制 | 探索基于神经网络的运动控制能力，从纯数字世界向物理世界延伸 |
| 硬件接口   | 支持通过工具调用控制外部硬件设备              |

---

## 版本时间线

```
v0.1 ─── v0.2 ─── v0.3 ─── v0.4 ─── v0.5 ─── v0.6 ─── v0.7 ─── v0.8 ─── v0.9 ─── v1.0
  │        │        │        │        │        │        │        │        │        │
  │        │        │        │        │        │        │        │        │        └─ 当前稳定版（见 版本日志.md）
  │        │        │        │        │        │        │        │        └─ 按会话模型与用量累计
  │        │        │        │        │        │        │        └─ 模式 / 推理 / 知识库 / 斜杠
  │        │        │        │        │        │        └─ 多标签、托盘、加密落盘
  │        │        │        │        │        └─ 长对话摘要流水线
  │        │        │        │        └─ 前缀缓存与侧栏用量
  │        │        │        └─ 协议、SSE、子进程、聊天/步骤 UI
  │        │        └─ 工具规范化
  │        └─ 本地文件与目录工具
  └─ 首版骨架

──────────────────────────────────────────────► 时间

                    ┌─────────────────────────────────┐
                    │     v2.0 多模态 + 记忆系统        │
                    │  语音 │ 图片 │ 记忆模型 │ 决策控制  │
                    │  RAG 增强 │ 嵌入式工具库 │ 运动网络 │
                    └─────────────────────────────────┘
```

---

## 版本号说明

| 版本   | 说明                          |
| ---- | --------------------------- |
| v0.x | 能力积累期，每期增加一个核心能力维度          |
| v1.0 | 首个稳定版（与版本日志 v1.0 条目对应）：上下文分段与用量体系等 |
| v2.0 | 多模态 + 记忆系统，从「工具助手」进化为「智能伙伴」 |

---

> 最后更新：与 `版本日志.md` 对齐整理（2026-05）；之后请以 `版本日志.md` 追加为准并定期同步本页归纳表。
